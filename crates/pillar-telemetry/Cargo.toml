[package]
name = "pillar-telemetry"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Vendor-neutral telemetry contracts and reference in-memory adapter. Port of pi packages/telemetry (pi v0.84.3)."

[dependencies]
async-trait = "0.1.89"
futures = "0.3.31"
thiserror.workspace = true

[dev-dependencies]
tokio = { version = "1.47", features = ["rt", "macros", "time"] }
