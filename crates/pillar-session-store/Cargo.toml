[package]
name = "pillar-session-store"
version = "0.1.0"
edition.workspace = true

[dependencies]
rusqlite = { version = "0.40", features = ["bundled"] }
pillar-agent = { path = "../pillar-agent" }
pillar-ai = { path = "../pillar-ai" }
serde_json.workspace = true

[dev-dependencies]
