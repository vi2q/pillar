# INSTRUCTIONS — 現在の状態・次の作業・移植の落とし穴

旧 `HANDOFF.md` の後継となる常時更新文書。規範は `AGENTS.md` と `docs/rules/` の方。ここには「現状」「次の作業」「移植時に判明した落とし穴」を書く。

## 運用ルール

- 作業区切りのたびに「現在の状態」「全体進捗」「次の作業キュー」「落とし穴」を更新する。旧 HANDOFF.md のようにセッション末尾に追記する運用はしない。
- 完了済み作業の履歴は git log に任せる。ここには残さない。
- 落とし穴は番号付きで管理する。コードコメントから `docs/INSTRUCTIONS.md #N` の形式で参照されるため、番号の欠番・再配置をしない。重複した項目は統合し、統合元の旧番号を併記してから整理する。
- 自動メッセージで "Please update docs/INSTRUCTIONS.md as needed." が届いたら、この文書が実態と乖離していないか確認し、乖離があれば修正する。
- 内容がコードと食い違い始めたら削り続けること (AGENTS.md: ドキュメントが劣化したら削除)。

## プロジェクトの目的

pi v0.84.3 (TypeScript, commit `56700d42e`) を Rust に移植する。拡張機能ランタイムだけは luaur v0.1.8 (Luau on Rust) に置き換える。ピンは `UPSTREAM.toml`。移植方針は `docs/rules/02-porting-policy.md` (外部境界は厳密互換、内部は慣用的Rust)。

上流チェックアウトは `/tmp/upstream/pi`, `/tmp/upstream/luaur`。無ければピン commit で再クローンする (docs/rules/06)。

## 現在の状態 (2026-09-02)

全ワークスペース 2,036テストがパス (protocol 49 / telemetry 15 / ai 345 / agent 301 / coding-agent 677 / tui 528 / client 49 / server 30 / session-store 35)。**pillar-session-store branch cache 層完了 (1d090d3)**: session-store/branch_cache.rs (branch-entries.ts 174行 + branch-tips.ts 35行 + branch-cache.ts 101行)。派生 root-to-tip branch cache — readCachedBranch (leaf の membership)、**queryCachedBranchRows (boundary subquery が最初の stop-at type/id で走査を止める — MIN/MAX aggregate を SQL text として interpolate。パラメータ文字列として bind すると SQL にならないのが今回の parity lesson)**、cursor/typed/custom-type filter、方向と limit、path materialization (**cycle 検出** + custom payload からの customType 抽出)、fork 用 copy-through-seq、branch tips (read/insert/**update は 'Branch tip changed during append' guard**/delete)、rebuild は canonical leaf から、appendEntryToBranchCache (root → 新 branch、tip 拡張、親を含む branch からの fork)。+ parity 11テスト。divergence: uuidv7 branch id は host 供給 (upstream は内部生成)、SessionError は同 kind 文字列 (storage/invalid_entry) のローカル StorageError。テストで確認した upstream 挙動: fork は親 seq まで source branch を copy、親 branch 欠損は 'no branch containing parent entry N'、cycle は 'Entry parent cycle at N'、boundary query は stop-at entry で止まる、custom entry は customType を cache に持つ。**pillar-session-store 新設 + SQLite schema/migration 層完了 (ca31b47)**: crates/pillar-session-store。packages/session-backends/sqlite-node/src/sqlite/migrations.ts + 埋め込み 001_initial.sql (122行) を移植 — sessions / entries (**session 毎 seq 一意 + id primary key**) / session_sequences / session_stats / branch_entries / lanes / branch_tips / writer_leases / migrations の各 table と index。applyMigrations — migrations table 生成、transaction 内での順次適用、ISO timestamp 付き id 記録 (外部 crate なしの civil date 計算)、適用済み id は skip。+ parity 7テスト。divergence: better-sqlite3 → rusqlite (bundled SQLite)、migration file の async fs 読み込みは include_str! 定数に置換。テストで確認した upstream 挙動: 新規 DB は全 migration を適用、適用は冪等、schema の全 table/index が存在、session id 重複と session 毎 entry seq 重複は拒否。**pillar-server PiServer 接続状態機械完了 (4aa14fe)**: server/server.rs。PiServer 接続状態機械 — accept (closing 中のサーバは新接続を即 close)、receive (decoder feed、無効 client data → failProtocol)、dispatchMessage (**awaitingHello は最初のメッセージが hello でなければならない**、2 回目の hello は失敗、ready は request を dispatch)、finishHandshake (protocol version 検証 — 'Unsupported protocol version N; expected 1' 文言、framed ServerHello 送出、stage → Ready)、failProtocol (stage → Closing、hello_error host event、最終 frame + close)、transport close、冪等 disconnect。constructor 検証 — 空 serverId と範囲外 maxFrameLength を upstream 文言で拒否。+ parity 9テスト。divergence: byte transport/listener、UUID 生成、handshake timeout timer は host-driven (timeout は deadline field)、outbound frame は socket 直書きでなく接続毎の vec に蓄積、request 実行は detached ConnectionState mirror で session id を同期。テストで確認した upstream 挙動: 最初の非 hello は 'The first client message must be hello'、2 回目の hello は 'hello may only be sent as the first message'、非対応 version は Version code で失敗、closing 中の accept は即 close、disconnect は冪等。**pillar-server snapshot publisher + protocol sanitizer 完了 (40c59bc)**: server/snapshots.rs。ServerSnapshotPublisher — get() は現在 revision で protocol version と server id 付き snapshot を構築、**broadcast() は ready 接続が存在し closing でない時のみ revision を increment**、ready 接続のみに server_snapshot event を送り、bump 済み snapshot を返す。protocol.ts の boundary sanitizer — sanitizeProtocolDetails (JSON passthrough、非有限の文字列化 path は serde_json 経由では到達不能と明記) と toProtocolUsage (**非負整数 clamp + floor、負 → 0、NaN cost → 0、reasoning は Option、cost は zero-fill**、AiUsage mirror struct 経由)。+ parity 10テスト。divergence: upstream の broadcast promise queue は同期の単調 revision guard に置換 (同一順序保証)、pi-ai model/message 型写像は host adapter 側。テストで確認した upstream 挙動: ready 接続なし/closing 中の broadcast は revision を進めない、ready のみ fan-out、負/NaN usage は 0 に clamp、token 数は floor。**pillar-server 新設 + live session manager 完了 (b57d560)**: crates/pillar-server。packages/server/src sessions.ts (346行) の LiveSessionManager 判定コアを同期 PiSessionRuntime trait 上で移植 — execute_command (list は live metadata を stored に merge、create はサーバ割当 id を service へ渡して attach + snapshot broadcast、attach は open + 再 attach で再 open しない、detach は attachment 解除 + 他接続があれば snapshot 再送 + maybeDispose、prompt/steer/abort/set-model/set-thinking は requireAttached で gate)、lifecycle (terminal で session_locked、id 不一致で invalid_request 'Service returned session X for server-assigned session Y'、operation count、**maybeDispose — idle + unattached + 操作なしで dispose + broadcast**、runtime error で terminate — 接続 close + 切断 + dispose)、normalizedSnapshot (phase は runtime から、attached は接続数、locked = true)。+ parity 11テスト。divergence: 非同期 acquire/dispose promise queue は同期化 (opening set + 明示 dispose)、broadcast/sendMessage callback は BroadcastSink trait、UUID 生成は host 供給 (execute_command が新 session id を受取)、接続 bookkeeping は host registry への id 参照。テストで確認した upstream 挙動: 未 attach の prompt は文言一致で失敗、runtime error は session を terminate + dispose、idle session は最後の detach で dispose するが active (Turn) は生存、live metadata は id で stored を上書き。**pillar-client PiClient request/response 層完了 (c0240ce)**: client/pi_client.rs。PiClient を Connection 上に移植 — request-id 追跡 (request() は connected 必須で framed RequestEnvelope を encode、complete_request() で ok/result または error により解決、未知 id は無視)、**close/error/disconnect 時の pending request 拒否は理由付き** (upstream PiDisconnectedError 意味論)、frame limit 検証 (整数 1..=u32::MAX、0 と 2^32 は拒否)、server event は host へ表面化、無効 protocol data で切断、handshake snapshot は take_handshake で poll、listener failure は接続状態に触れずに計数 (upstream onListenerError isolation)。+ parity 13テスト。divergence: pending promise は take_pending_failures() で drain する pending map、response は callback でなく host 向けに蓄積。テストで確認した upstream 挙動: close/error/disconnect で各理由付き reject、list 応答で pending 解決、error 応答は server code+message 付きで解決。**pillar-client session-handle + unix transport 完了 (57a4195)**: client/session_handle.rs — SessionHandle を client state 上で移植 (attachment/snapshot getter、prompt/steer/abort/set-model/set-thinking を pillar-protocol Command enum に写像 — 各メソッドは result.session を返す、request pipeline は host 供給 closure、lease mode 既定 shared、PiSessionDetachedError 文言一致)。client/unix.rs — option 検証 (空 path、107/103 バイトの platform 別 path 上限、zero maxPendingBytes、既定 4x frame length) と std::os::unix::net 上の UnixByteTransport — pending-byte limit 会計、冪等 close、closed transport の send エラー、read_inbound host ヘルパ。+ parity 17テスト。divergence: 書き込み queue の promise tail と drain 会計は同期 blocking write に置換 (pending limit は queue された 1 書き込みごとに適用)、connect 時の callback fan-out (onData/onClose/onError) は host の event loop。テストで確認した upstream 挙動: 空/超過/zero-pending option は文言一致で拒否、close 後の send は失敗、detached error は 'Session <id> is not attached'。**pillar-client connection state machine 完了 (3733229)**: client/connection.rs。Connection 状態機械を注入 ByteTransport trait 上で移植 — connect (pillar-protocol codec で client hello を encode、Connecting 状態 + state change event)、attach_transport (stale connection id は新 transport を即 close)、handle_data (decoder feed、hello 前の data で fail、stale-id guard)、handle_message (**hello で handshake 完了 — snapshot を take_handshake 用に保存**、hello_error はサーバメッセージ付きで fail、最初の非 hello メッセージで fail、connected 後の handshake message で fail、他は ConnectionEvent::Message として表面化)、handle_close/handle_error (stale-id guard)、明示 disconnect (冪等 — 2 回目で Failed event なし)。+ parity 8テスト。divergence: ByteTransportFactory promise と handshake promise resolver は host-driven — transport は open 後に attach、解決済み snapshot は take_handshake で poll、event は callback でなく drainable vec に蓄積。テストで確認した upstream 挙動: client hello 前の data で fail、hello_error はメッセージを表面化、最初の非 hello は 'Expected server hello' で fail、stale id は無視、transport close は 'Byte transport closed'。**pillar-client 新設 + client state machine 完了 (37fb7c7)**: crates/pillar-client。packages/client/src state.ts の ClientState を pillar-protocol 型上で移植 — server snapshot (stale revision 拒否、同 revision は適用)、session snapshot (session 毎 revision gate + force override)、attachment bookkeeping (applyResult: list 無視、detach は attachment 解除 + snapshot を detached 化、create/attach/prompt/steer/abort/set-model/set-thinking は session 更新)、applyEvent の session scoped event routing (session_snapshot/progress/removed が session id を持つ)、restoreSessionSnapshot (未知 session のみ)、reset/clearAttachments/dispose、listener fan-out + unsubscribe (空 session map の cleanup)。errors.ts — PiServerError/PiDisconnectedError/PiClientDisposedError/PiSessionOwnershipError/PiSessionDetachedError。+ parity 11テスト。divergence: connection/transport 層 (connection.ts、unix socket、promise resolvers、ByteTransport) は host 側、listener error は収集に変更。テストで確認した upstream 挙動: stale snapshot は force なしで無視、detach は保存済み snapshot の attached flag を反転、session listener は scoped。**pillar-tui terminal-colors 完了 (11b84f4)**: tui/terminal_colors.rs。parseOsc11BackgroundColor — hex #rrggbb、16-bit #rrrrggggbbbb (channel 毎に 8-bit へ丸め)、rgb:/rgba: hex channel (任意幅、16^len-1 で scale)、BEL/ST 終端の strict shape、isOsc11BackgroundColorResponse、parseTerminalColorSchemeReport (繰り返し CSI ? 997 ; 1|2 n — 最後の report が勝つ、未知 code / query sequence / junk prefix は拒否)。+ parity 7テスト。divergence: query lifecycle (pending replies、timeout timer、terminal 書き出し、listener fan-out) は host 側、純粋 parser のみ移植。upstream terminal-colors.test.ts anchor: rgb:0000/8000/ffff → (0,128,255)、非 strict 形は拒否、繰り返し report は最後が勝つ。**pillar-tui editor autocomplete 判定コア + bounded writer 完了 (6497639)**: tui/editor_autocomplete.rs。TriggerPattern (最後の非空白 token に対する symbol check)、DebouncePattern — @-attachment regex: **引用符で閉じていない quoted path は空白を含み得る**ため、@ token は最後の空白位置でなく blank 区切りの @ 開始位置で特定 (テストで修正したポイント)、trigger 文字登録 (既定 @ #、/ と whitespace は除外、重複除去)、getAutocompleteDebounceMs (明示 Tab / force は即時、進行中の @ token は 20ms)、SlashMenuContext (行 0 のみ、空白 or / 開始)、getBestAutocompleteMatchIndex (完全一致 > 先頭 prefix)、createAutocompleteList (slash prefix は 12..32 primary 列 layout — 2カラム path は description ありの時だけ活性化)。BoundedTerminalWriter (tui-main-screen.ts) — 1MiB chunk 分割 (upstream は surrogate pair 非分割、Rust UTF-8 は不要)。+ parity 13テスト。divergence: 非同期 request pipeline (AbortSignal 付き getSuggestions、debounce timer、request task chaining、request-id staleness) は host 側。テストで確認した upstream 挙動: 閉じ quote ありの @ token は debounce しない、完全一致は最初の prefix match に勝つ、oversize render はちょうど 1MiB 境界で分割。**pillar-tui overlay focus machine 完了 (bcd395e)**: tui/overlay_focus.rs (tui.ts TuiBase の overlay stack + focus 状態機械)。OverlayFocusMachine — component id ベース (host 供給の mount graph が Container 子ツリー相当)。show/remove/hide-top は pre-focus chain と retargetPreFocus (削除された overlay を preFocus に持つ entry はその preFocus を引き継ぐ) を扱い、remove はエントリの preFocus を削除前に capture して fallback を正しい target に向ける。setFocus — eligible/blocked focus-restore 遷移: 可視 overlay への focus は Eligible を記録、base focus は overlay が pre-focus ancestor chain にいない時のみ Blocked (upstream isOverlayFocusAncestor — editor が overlay の preFocus なら blocked にせず Eligible を温存)、blocked resume は RestoreOverlay/FocusTarget を解決し blocked component の mount 状態で gate。routeInputFocus — focused overlay が不可視化されたら topmost 可視か preFocus へ redirect、その後 eligible/blocked 規則を適用。setHidden (hide は focus を topmost/preFocus へ、show は capturing のみ focus order を進めて再 focus、non-capturing は自動 focus しない)、unfocus、hasOverlay、isOverlayFocused。+ parity 13テスト (overlay-non-capturing.test.ts の anchor 群)。divergence: component identity は u64 id、render scheduling と terminal 書き出しは host 側 (op は hide-cursor / request-render ヒントを返す)。テストで確認した upstream 挙動: non-capturing overlay は生成時に focus を保持、stacked overlay は段階的に復帰、削除された focused sub-overlay は親 overlay → editor の順で fallback。**pillar-tui virtual terminal 完了 (42fa83c)**: tui/virtual_terminal.rs (test/virtual-terminal.ts 218行の Rust 版 screen model)。TUI renderer が発行する制御 sequence を解釈 — CUP (1-indexed row;col)、cursor up/down、CHA、erase line (0=cursor 以降 / 2=全行)、erase display (2=viewport)、mode set/reset (?2004 paste、?25 cursor、?2026 synchronized output、?2031 — inert)、OSC title (無視)、APC (CURSOR_MARKER — zero width)。viewport + scrollback buffer — cursor が viewport 最下行を超えると scroll (LF 後に新 blank 行が最下段に来る xterm 挙動)、幅超過行は truncate、buffer 座標での cursor 追跡。send_input は host handler へ、resize は handler 通知、reset で blank 化。+ parity 18テスト。divergence: xterm.js の完全 emulation (SGR 状態、wide-char cell grid、scroll region) は範囲外 — renderer が実際に発行する sequence のみ解釈 (parity suite の比較に必要な部分)。テストで確認した挙動: 最終 LF 後は viewport 最下段が blank、erase-from-cursor は prefix 保持、ESC[2J で viewport 空化、CUP 後の cursor は書き込み位置の次。**pillar-tui terminal negotiation コア完了 (e3379d1)**: tui/terminal.rs (terminal.ts 553行の判定部)。parseKeyboardProtocolNegotiationSequence — Kitty flags (CSI ? <n>u) と Device Attributes (CSI ? [;d]*c、空 body も受理)、prefix 検出 (CSI ? [;d]* で reassembly 待ち)、Kitty keyboard protocol query 発行 (flags 1|2|4)。KeyboardProtocolNegotiator — buffer 化された negotiation 状態機械: 部分応答は feed を跨いで完成、放棄された prefix は host 転送用に flush_pending で返却、flags≠0 で Kitty protocol 有効 + modifyOtherKeys 無効、flags=0 または Kitty 応答前の DA で modifyOtherKeys へ fallback、後から来た Kitty 応答が優先。resolveEscapeTimeoutMs (PI_TUI_ESC_TIMEOUT override、SSH 検出で 100ms、既定 10ms)、shift+enter 非報告端末向けの CR 書き換え、Apple Terminal セッション判定。+ parity 15テスト。divergence: ProcessTerminal (raw mode、SIGWINCH、bracketed paste 有効化、progress keepalive OSC 9;4、Windows VT input、write log) は host 側、タイマーは host-driven (pending 返却 + 明示 flush_pending)。テストで確認した upstream 挙動: Kitty CSI-u キー (ESC[97u) は negotiation 応答ではない、分割応答は feed を跨いで完成、modifyOtherKeys 有効後の Kitty 応答が優先。**pillar-tui alt-screen diff コア完了 (662ea0a)**: tui/alt_screen.rs (tui-alt-screen.ts 1378行の doRender 判定部)。classify_alt_screen_diff — 初回 render か寸法変更で full redraw、変更 row のいずれかに Kitty/iTerm2 image line が絡む (新旧どちらでも) なら ImagesNeedRedraw、それ以外は row updates。rows_to_paint — redraw 時は全 row、local 更新時は変更 row のみ。image_clear_prefix — full redraw は upload 存在時 Kitty placement 全削除 (deleteAllKittyPlacements)、image diff は protocol 别 clear (iTerm2 は ESC[2J、kitty は placement 削除)、row updates は何もしない。cursor_position_suffix — 絶対 CUP (col は min(width, col)+1 の 1-indexed、テストで clamp が col 81 を出すことを確認) + hardware cursor show/hide、marker なしは hide。row_update_escape — 行単位の CUP + clear line + content。prepare_kitty_screen — upload registry の visible-id 追跡、同一 generation の再登録、offscreen eviction (3 予算: 5 枚 / 30MiB transmission / 300MiB decoded、最古から、可視 image は絶対 evict しない)。+ parity 20テスト。divergence: layout pipeline、search/selection/flash composite、mouse handling、terminal 書き出しは host 側、Kitty placement replacement (getKittyImagePlacement による transmission → placement-only 書き換え) は upload 側の管理と共に host。テストで確認した upstream 挙動: image line は新旧どちらかにあれば全行 repaint、6 枚目の offscreen upload で最古を evict、可視 image は予算超過でも温存。**pillar-tui main-screen render 判定コア完了 (6fdfbfc)**: tui/main_screen.rs (tui-main-screen.ts 654行の判定部)。rendered 行からの Kitty image header パース (i= ids / r= rows、既定と検証)、reserved-row bookkeeping (getKittyImageReservedRows — 宣言 row 数を残り行数で bound、空行のみ拡張)、diff range の image block 全体への拡張 (expandChangedRangeForKittyImages)、変更範囲に絞った delete sequence (deleteChangedKittyImages)。changed_range — 最初/最後の変更行 + appended 処理 (append は新しい tail に強制)、is_append_start (前 content の正確な末尾からのみ fast path)。decide_render — doRender の決定木を純粋関数化 (MainScreenRenderState 追跡): 初回 render、幅変更、高さ変更 (Termux はスキップ — SW keyboard で高さトグル)、clear-on-shrink (high-water mark 超の縮小 + overlay なし + flag on)、変更範囲が前 viewport 上端より上 (差分は可視行しか触れない → 全再描画)、deleted-only、no changes、differential。+ parity 25テスト。divergence: terminal 書き出し (BoundedTerminalWriter 1MiB chunk、synchronized output wrap、cursor 移動 escape、positionHardwareCursor) と TuiBase scheduling は host 側。テストで確認した upstream 挙動: Termux は高さトグルを跨いで differential (同一内容は no-op)、行 unchanged の縮小でも high-water mark で clear、deleted-only は full clear と区別。**pillar-tui overlay layout コア完了 (3550d7f)**: tui/overlay.rs (tui.ts の overlay 判定部)。resolveOverlayLayout — 既定幅 min(80, available)、SizeValue の % パース (小数1桁精度)、minWidth 引き上げ、margin 調整後の available へ clamp、maxHeight 切り詰め、9 anchor の row/col テーブル、絶対値/% の row・col override、offsetX/Y、margin 考慮の端末境界 clamp。prepareOverlay (2段階 resolution: 幅/maxHeight を先に、render 後の高さで位置を再解決 — upstream compositeOverlays の pre-render loop) と compositeOverlays (max(content, 端末, 必要行) に pad — 上位 mark の自己強成的 inflation は除外、viewportStart offset、合成前の横幅超過行を防御的に truncate)。extractCursorPosition (下端 viewport のみ CURSOR_MARKER 走査、可視 col 計算、marker 除去)、applyLineResets (末尾 SEGMENT_RESET、image line passthrough)。+ parity 17テスト (lib 内テスト)。divergence: TuiBase lifecycle (render scheduling、terminal wiring、overlay focus-restore 状態機械、OSC 11 / color-scheme / cell-size query) は host 側、純粋な layout 計算と composite のみ移植。upstream overlay-options.test.ts の anchor: 幅10/80列で中央 col 35、bottom-right は最終行末、margin 5 で (5,5)、負 margin は 0 に clamp、100列の 50% は幅 50、minWidth は小さい % を引き上げ、row 100% は最下段。**pillar-tui layout engine 完了 (1365722)**: tui/layout.rs。layout.ts の layout engine — component tree を固定サイズの screen 行列へ配置。leaf box (render + cursor marker の line offset — CURSOR_MARKER 行が見えるよう長い content を scroll)、scroll container (子を y - scrollTop に layout、metric 更新、符号付き delta で子を再 translate、clip 再計算)、vstack (basis 考慮の高さ、grow/shrink 配分、gap 行)、hstack (render 結果から計測した basis 考慮の幅、stretch/center/end 垂直 align、幅 0 の子は空 rect)。paintBox — clip 付き行描画 (compositeTuiLine)、未触の行への全幅 fast path、styleScrollbarCell (grapheme/ANSI 対応 cell restyle) による scrollbar 描画。getScrollbarGeometry (thumb 高さ = max(min(2,track), track²/content) clamp、scrollTop 比例の thumb 位置、最終列の clip 含有チェック)。getScrollViewsAt (hit testing、内側優先)。+ parity 19テスト。divergence: LAYOUT_NODE symbol protocol は closure ベースの LayoutNode enum に置換 (ポートの closure component 規約に整合)、scroll state は RefCell 共有 (upstream は component identity 経由で mutate)、Kitty image crop と OSC 133 zone prefix は host 側、render cache は component identity でなく node path キー。テストで確認した upstream 挙動: follow-end は初回 layout で metrics 更新後に pin、thumb は track²/content で丸め、hit test は clip 外で空。**修正 (e42e707)**: editor コア作業中に crates/pillar-tui/src/keybindings.rs を上書きしてしまい、coding-agent の core/keybindings.rs が消費する BTreeMap 形 API (tui_keybindings() -> BTreeMap、default_keys: Vec<String>、description: Option<String>) が壊れた。cd80e00 の元実装に戻して復旧 — 同じ 9-test parity suite がそのまま通り、全ワークスペースのテストがクリーン。**教訓**: 新規モジュールを書く前に `git log -- <path>` で先行実装の有無を必ず確認する (keybindings_parity.rs だけが残っていて module が無いように見えたのは、確認不足だった)。
- **テストの共有 temp dir は並列実行で競合する** (0224d12) — process id だけでは不十分。`SystemTime` の subsec_nanos 等で呼び出し毎に一意化する。フレークしたらまず共有状態を疑う。**pillar-tui stdin-buffer 完了 (7aee065)**: tui/stdin_buffer.rs。stdin 入力を buffer して完全な escape sequence のみ発行 — CSI (final byte 0x40-0x7E、SGR mouse `<B;X;Y[Mm]` 構造検証)、OSC/DCS/APC (ST/BEL 終端)、SS3 (ESC O + 1 文字)、旧式 mouse (ESC[M + 3 バイト)、meta key (ESC + 1 文字)。extractCompleteSequences — 位置毎に候補を伸長、未完 tail は remainder 保持、WezTerm の二重 escape 対応 (`\x1b\x1b` + 新 sequence 開始文字なら先頭 ESC を単独発行)。bracketed paste — chunk 跨ぎ buffer、marker 前の入力は sequence 処理、paste 内容は paste event、後続入力は再処理。Kitty printable dedup — 非修飾 CSI-u 印字 codepoint で pending 設定、一致 raw 文字は抑制、他入力で解除。timeout — 単独 ESC は escape timeout (10ms)、部分的 CSI は sequence timeout (50ms)、host が schedule する flush deadline として露出。+ parity 26テスト。divergence: EventEmitter と setTimeout は host-driven (process_with_clock が deadline を返す、flush は明示呼び出し)、Buffer の high-byte 変換は host 側。テストで確認した upstream 挙動: 旧式 mouse は ESC[M + 3 バイト = 6 文字で完全、chunk 跨ぎ CSI は 2 回の process で 1 sequence に結合、paste 後の残入力は通常 sequence として発行。**pillar-tui editor コア完了 (635246e)**: tui/editor.rs (editor.ts 2363行)。複数行 editor state、fish-style undo coalescing (連続 word 文字は 1 unit、空白は各々個別 undo)、kill ring 蓄積、prompt history (連続重複スキップ、100 件上限、draft 復帰)、char jump (複数行、cursor 位置スキップ)。wordWrapLine (word 境界 backtrack — upstream は wrap 機会 index まで slice するので chunk に末尾空白が含まれる、長語は文字単位 break、CJK break 機会、chunk index は行を分割)。paste marker — 大きい paste (>10 行 or >1000 文字) は atomic な [paste #N ...] marker + registry、backspace は registry エントリ削除 + counter 減 + 上位 id shift + テキスト内 marker renumber、bracketed paste 内の CSI-u 制御バイトをリテラル ctrl 文字へ decode、word 文字直後の path paste は先頭 space 付与。visual line map + sticky column decision table (upstream computeVerticalMoveColumn)、atomic segment への cursor snap (下向き移動時は continuation VL をスキップ)、rewrap 後の snappedFromCursorCol 解決。setText/insertTextAtCursor (undo atomic)、paste marker 展開付き submit、backslash-Enter submit ゲート。+ parity 54テスト。divergence: TUI render loop、autocomplete 要求スケジューリング (debounce/abort/async provider)、keybinding dispatch は host 側、Intl.Segmenter は grapheme/word ヘルパ + marker merge で置換。テストで確認した upstream 挙動: wrap chunk は末尾空白を含む、4096 ちょうどの paste は marker 化しない境界、縦移動は visual col を保持し short line で clamp 後 sticky 復帰、歴史 down で draft 復帰。**作業メモ**: /tmp/upstream/pi は途中で消える/壊れることがある — docs/rules/06 の pin commit から git archive で再抽出した (`git archive <pin> packages/tui/src | tar -x -C /tmp/`、checkout は guard で弾られるため)。**keybindings conflict 順序修正**: user claim の競合検出を BTreeMap にして決定順序化 (upstream は JS object 挿入順)。**pillar-tui keybindings registry 完了 (55a850b)**: tui/keybindings.rs。TUI_KEYBINDINGS 全テーブル (editor 操作/編集、input actions、selector、alt-screen viewport 操作の 51 アクション、既定キー + description)。user override はアクション単位で既定を置換 (normalizeKeys で重複除去)、conflict は user claim が同一キーに複数アクションを束ねた場合のみ検出 (デフォルトは他アクションとのキー再利用で evict されない)。matches/getKeys/getDefinition/getConflicts/getResolvedBindings/setUserBindings/getUserBindings、thread-local global manager は with_keybindings クロージャ API。+ parity 9テスト (既存 keybindings_parity.rs が参照していた予定 API に実装を整合)。divergence: 下流パッケージ向け declaration merging 拡張点は static テーブルに還元。テストで確認した upstream 挙動: ctrl+j は newline の既定エイリアス、submit 再束置でも select.confirm の enter は生存、直接の user conflict は報告するが既定は温存。**pillar-tui terminal-image 完了 (761f981)**: tui/terminal_image.rs (base64 クレート新依存)。ターミナル capability 検出 (kitty/ghostty/wezterm/warp → kitty protocol、iTerm2、tmux は images 無効 + hyperlink は転送確認時のみ、screen は hyperlink 無効、vscode/alacritty/WT_SESSION は images 無効 + hyperlink 有効、jetbrains-jediterm と unknown は保守的)、PI_* 相当の CapabilityOverrides、capability cache の set/reset/override。cell dimensions (既定 9x18px、host 設定)、アスペクト維持の min-scale cell sizing + clamp、calculateImageRows。Kitty graphics encoding (a=T,f=100,q=2、C=1 cursor 抑制、c/r/i、4096 バイト chunking と m=1/m=0 継続、delete コマンド 3 種)、iTerm2 encoding (params は ';' 結合、データ直前は ':')、PNG (IHDR BE@16) / JPEG (SOF0-2 走査) / GIF (LE@6/8) / WebP (VP8/VP8L/VP8X) の dimension probe、Kitty placement metadata registry (generation 加算、id 置換、1000 超で最古 evict、行内 i= 制御で lookup)、OSC 8 hyperlink wrap、image text fallback。+ parity 36テスト。divergence: 環境読み取りは DetectionEnv で host 供給 (process.env は実行時状態のため)、image ID は時間ベース (rand 非依存)、Image component wrapper と renderImage 組立は TUI 統合待ちで host 側。テストで確認した upstream 挙動: tmux の hyperlink ゲート、4096 境界ちょうどは単一 chunk、iTerm2 の name は base64、fallback は絶対パスを file:// hyperlink 化。**pillar-tui loader 系 + scroll-view 完了 (10c9b9e)**: tui/loaders.rs。Loader (braille 既定 frame セット 80ms、custom frames/interval、空 frames で indicator 非表示、custom indicator は spinner color をバイパスし verbatim 描画、Text margin 由来の先行空行、host-driven tick で frame wrap)、CancellableLoader (abort 状態、keybinding 判定は host 側)、AltScreenFlashContainer (inverse-video の transient メッセージ、幅 truncate、既定 1000ms、host-driven expire、複数エントリ順序保持)、ScrollView (clamp 付き scrollTo + disableFollow による end 位置での follow 抑制、未消費分を返す scrollBy、layout 更新で追従する follow-end pinning、scroll-to-start/end、auto/always/hidden scrollbar + 1 列予約 + 行末スペース pad、transient auto scrollbar の可視性と host-driven hide timer)。+ parity 31テスト。divergence: アニメーション/タイマー callback は host-driven の explicit メソッド、keybinding dispatch は host 側、LAYOUT_NODE protocol は未移植 (子は closure で render)。テストで確認した upstream 挙動: 最後の braille 点の後に先頭へ wrap、overscroll 余剰算術、follow-end は content 増加で再 pin するが scroll-by 後と disableFollow 後はしない、auto scrollbar は遅延後に隠れ content が viewport 収まる時は表示されない。**pillar-tui markdown 完了 (86e2573)**: tui/markdown.rs (pulldown-cmark を新依存に追加)。heading/paragraph/code/list/blockquote/table/hr/html ブロック、inline strong/em/codespan/link/del + ANSI reset 後の style-prefix 復元 (upstream InlineStyleContext)、(text, width) render cache。ストリーミング中の partial closing fence を source level で trim (upstream trimPartialClosingFences 相当: 最終行が 3 未満の fence 文字連続で先行 fence があり EOF が行途中のとき)。list は ordered/unordered、開始番号、marker 保持 option、task marker、depth あたり 4 スペース indent、continuation prefix 折り返し、pulldown の Paragraph item を loose 判定。table は border overhead 3n+1、natural/min-word 列幅 (最大 unbroken word 30)、weight ベース縮小、端数分配、太字 header、cell 折り返し、狭すぎる場合は raw markdown fallback。+ parity 41テスト。divergence: marked の tokenizer extension は pulldown で代替 (LaTeX は latex.rs の render_latex に pending passthrough で routing)、OSC-8 hyperlink と image line は host 側のため link は常に URL fallback path、strict-strikethrough は GFM option で近似。テストで確認した upstream 挙動: level 3 以上の見出しは # 接頭辞を保持し 3 未満では落とす、partial closing fence は完成 fence と同一出力、blockquote 内では default text style を抑制、狭すぎる table は raw fallback、hr は 80 列上限、空入力は 0 行。**pillar-tui input + settings-list 完了 (43d8337)**: tui/input.rs + tui/settings_list.rs + keys.rs printable decoder 追加。decodeKittyPrintable (CSI-u から印字文字抽出 — plain/Shift のみ、Shift 時は shifted keycode を優先、Ctrl/Alt/Super ビットと制御 codepoint は拒否)、decodeModifyOtherKeysPrintable (shift のみ許可)、decodePrintableKey。Input: 单行テキスト入力 — grapheme 単位のカーソル移動/backspace、word 文字連続を 1 unit に coalesce する undo (空白で unit 分割)、kill ring (連続 kill を蓄積、yank/yank-pop)、bracketed paste (改行除去、tab→4スペース)、カーソル中心の横スクロール、focused 時 CURSOR_MARKER 付き inverse-video fake cursor 描画。SettingsList: label 列 pad (max 36)、値 cycle (wrap + change callback)、スクロール window、選択行の description フッタ、hint 行、fuzzy search モード (埋め込み Input、filter で選択リセット)。+ parity 44テスト。divergence: keybinding テーブル dispatch は host 側、編集/選択プリミティブは plain メソッドとして公開、submenu は state + done-callback に還元。テストで確認した upstream 挙動: abc は 1 undo unit で空白が unit を分ける、連続 word kill は 1 yank entry に蓄積、shift 付き CSI-u は shifted keycode を返す、cursor は value 先頭の最初の文字の上に描画される。**pillar-tui select-list 完了 (10a59c8)**: tui/select_list.rs。大文字小文字を無視する value 前方一致フィルタ (filter 変更で選択リセット)、selected-index clamp、wrap する up/down 移動、選択位置中心のスクロール window + (n/total) インジケータ、2カラム layout (label 列は最広 item に合わせ min/max clamp、description 列は 1 行に潰して truncate) — 幅 40 以下では 1 カラム。custom truncate_primary hook は context (text/maxWidth/columnWidth/item/isSelected) を受け取り、結果は maxWidth に再 truncate。+ parity 18テスト。divergence: keybinding dispatch (handleInput) は host 側、theme hook は boxed closure。テストで確認した upstream 挙動: 選択は index 0 初期なので先頭 item が selected prefix で描画される、description の改行連続は 1 スペースに潰れる、2カラム path では primary 列幅ぶん description が pad される。**pillar-tui stack layout 完了 (15ea391)**: tui/stack_layout.rs。sliceByColumn/sliceWithWidth (可視列 range 抽出、strict で境界を跨ぐ wide char 除外)、extractSegments (overlay 領域前後を 1 pass で抽出 — after は overlay 前の SGR 状態を継承)、compositeTuiLine (segment reset でスタイル混入を防止して overlay 合成)、Stack flex アルゴリズム — allocateStackSizes (basis/intrinsic を min/max で clamp、gap 差し引き後の grow/shrink 配分) と distribute (weight 比例で per-round 配分、capacity で制限)。VStack/HStack render (render closure ベース: 縦は gap 行 + サイズ pad、横は center/start/end 垂直 align)。+ parity 19テスト。divergence: LAYOUT_NODE render protocol と Container lifecycle は host 側、image-line passthrough は未対応 (terminal-image host 側)。テストで確認した upstream 挙動: distribute ループの per-round weight share は 1:3 / 8列 が 3:5 に収束、overlay は毎回 segment reset で囲まれ、overlay 後の base tail は保持される。**pillar-tui truncate + components 完了 (88f4006)**: text_utils.rs に truncateToWidth / truncateFragmentToWidth / finalizeTruncatedResult (ellipsis 付き可視幅切り詰め — ASCII fast path / grapheme path / ANSI+tab 対応パスの contiguous prefix ルール、pad-to-width、ellipsis 前後の reset code)。components.rs — Text (word wrap + margin 自動縮小、tab 正規化、text+width キーの cache、background 適用)、Spacer、Box (子の padding + background、bg 変更は出力サンプリングで検出、childLines+width+bgSample キーの cache)、TruncatedText (1行・newline で停止・幅 pad)。+ parity 16テスト。divergence: upstream は Component trait + 共有 TUI render loop、ポートは子を render closure で受ける (型が異質なため)。テストで確認した upstream 挙動: finalizeTruncatedResult は ellipsis を reset code で挟む、Box の bg サンプル変更で cache 無効化。**pillar-tui text-utils 完了 (07963a7)**: tui/text_utils.rs (utils.ts の text-metrics コア)。extractAnsiCode (CSI m/G/K/H/J、OSC BEL/ST、APC、2文字エスケープ)、stripTerminalSequences、graphemeWidth (string-width 互換分類: zero-width/control/mark、terminal-spacing mark、RGI emoji 2セル、East Asian wide/fullwidth、regional indicator)、visibleWidth (tab=3、bounded cache)、AnsiCodeTracker SGR ステートマシン (256色/RGB パラメータ消費、属性別 reset、OSC 8 hyperlink は SGR reset を跨いで保持、行末 underline reset)、OSC 8 open/close 形式、splitIntoTokensWithAnsi (CJK break point)、breakLongWord、wrapTextWithAnsi (ANSI 保持 word wrap — padding/background なし)。+ parity 16テスト。divergence: Intl.Segmenter grapheme clustering と v-flag Unicode property regex は端末関連クラスに絞った文字分類テーブルに置換。**pillar-tui autocomplete 完了 (8c0fb10)**: tui/autocomplete.rs (autocomplete.ts 826行)。パス prefix 抽出 (未閉鎖 quote トークン + @ 付き、区切り文字考慮の token start、自然 trigger と Tab 強制)、slash コマンド候補フィルタ + argumentHint と description の合成 ("hint — desc")、ディレクトリ列挙候補 (~展開、~/ と ./ prefix 保持、symlink→dir 判定、dir 先頭 + アルファベット順ソート)、fuzzy file スコアリング (完全一致 100 / prefix 80 / filename 部分一致 50 / path 部分一致 30、dir +10、depth/長さ/辞書順タイブレーク)、scoped fuzzy query と表示パス書き換え、CombinedAutocompleteProvider の判定フロー、applyCompletion のテキスト操作 (slash コマンドは末尾 space、@ 添付は dir で連続補完可能、quoted prefix の重複 quote 除去)。+ parity 15テスト。divergence: fd walk は host 注入コールバック (upstream はプロセス spawn)、abort signal は引数チェックに縮約、引数補完は host 駆動。テストで確認した upstream 挙動: bare "/model " の後の Tab はコマンド扱い (trim が末尾 space を除去)、行中 @ 補完は cursor 後の原文をそのまま保持。**pillar-tui latex 完了 (c3906a0)**: tui/latex.rs (latex.ts 1380行)。LaTeX → 端末向け Unicode 数式レンダラ。シンボル表 (ギリシャ/演算/矢印/関係/否定/黒板/上下付/アクセント — 文言完全一致)、named operators、limit operators は display layout で積み上げ、spacing/size/ignored コマンド群、\not 否定 (combining slash)、インライン分数 (slash 形 + 括弧ルール)、sqrt (次数オプション)、mathbb、アクセント (1文字は combining、複数は関数形)、operatorname、pmod/pod、overset/underset、環境 (equation、align 系、cases の ⎧⎨⎩、matrix 系は列幅整列 + pmatrix/bmatrix/Bmatrix/vmatrix/Vmatrix デリミタ)、script は Unicode マップ → prefix フォールバック、normalizeOutput (マーカ除去 + spacing ルール + whitespace 圧縮)、スタック layout レンダラ (分数の ─ バー、operator limits、matrix、ベースライン整列)。+ parity 24テスト。divergence: Unicode property regex は文字分類へ置換、ネスト render は新規ノードリストを所有し marker index を base 分 rebase。テストで確認した upstream 挙動: 行端の relation/operator spacing は normalizeOutput で trim される (×/·/≤ は外側空白なし)、\mathbb は全黒板文字をマップ (ZR→ℤℝ)、マップ外 sup は prefix 形。**pillar-tui alt-screen-search 完了 (14bea17)**: tui/alt_screen_search.rs (alt-screen-search.ts 157行の match コア)。stripTerminalSequences (CSI/OSC/2文字エスケープ除去)、buildSearchCorpus (文字毎の source span マップ付き — 端末シーケンス除去、whitespace run は span なし単一セパレータへ圧縮、行境界も whitespace 扱い)、normalizeQuery (whitespace 圧縮 + trim)、findAltScreenSearchMatches (大文字小文字非依視、非重複、一致文字を連続 row/col セグメントに再統合)、getAltScreenSearchMatchKey (firstRow:firstStartCol:lastRow:lastEndCol)。+ parity 9テスト。divergence: 対話検索 UI コンポーネント (Input + ハイライト描画) は host 側、grapheme セグメンテーションは char 反復にマップ。**pillar-tui undo/kill/word-nav 完了 (06aba55)**: tui/edit_support.rs — undo-stack.ts (clone-on-push スナップショット、pop は分離済みエントリを返す、clear)、kill-ring.ts (空テキスト無視、accumulate は最新 entry へ merge — 後方削除は prepend/前方削除は append、peek、rotate で yank-pop サイクル)、word-navigation.ts (findWordBackward/findWordForward: 末尾/先頭 whitespace をスキップし 1 回に 1 セグメント — word-like は ASCII 句読点境界を保持、句読点 run、atomic セグメントは単一ユニット)。+ parity 10テスト。divergence: Intl.Segmenter は Unicode 英数字クラスによる word/punct/ws run セグメンタに置換、undo スタックは structuredClone でなく Clone (エディタ状態は値型)。テストで確認した upstream 挙動: ワード移動は 1 呼び出し 1 セグメント (句読点 run は次の hop でスキップ)、atomic は現セグメント時に全体スキップ。**pillar-tui fuzzy 完了 (893edbe)**: tui/fuzzy.rs (fuzzy.ts 137行)。部分一致 fuzzy match (連続ボーナス -5/step、gap ペナルティ +2/文字、word boundary ボーナス -10 — 先頭と space/-/_/./:/ の直後、位置ペナルティ +0.1/index、完全一致 -100、英数字スワップ再試行 abc123↔123abc は +5 ペナルティ)、トークン化フィルタ (空白/スラッシュ区切りトークンがすべて一致必須、スコア合計で昇順ソート)。+ parity 14テスト。divergence: 上流の JS regex による英数字分割はバイトレベル分類に置換、フィルタは text accessor 付き &[T] で公開。**footer-data-provider 解決コア完了 (d6ae8f4)**: core/footer_data_provider.rs (footer-data-provider.ts 388行の解決部)。git branch 読み取り (通常 repo の HEAD `ref: refs/heads/<branch>`、detached HEAD は "detached"、reftable の `.invalid` マーカーは `git --no-optional-locks symbolic-ref --quiet --short HEAD` へフォールバックし失敗時 "detached"、worktree gitdir HEAD)、WSL 環境 + `/mnt/<drive>` パス判定による HEAD polling ゲート、フッタ状態管理 (拡張ステータス set/remove/clear、利用可能 provider 数、branch キャッシュ + invalidate、branch-change コールバック登録/解除)。+ parity 9テスト。divergence: ファイル監視 (fs.watch / watchFile / reftable watcher と retry timer) は host イベントループの関心事で host が notify_branch_change を駆動。**remote-catalog-provider decision コア完了 (d3bc036)**: core/remote_catalog_provider.rs (remote-catalog-provider.ts 137行の純粋判定部)。mergeModels (dynamic overlay は同 id を in place 置換、他は追加)、parseCatalog (bare array / models 包裹 object / 汎用 object map、id 持ち object のみ、provider 強制、部分的 entry は構造既定値で埋める — upstream の緩い Model スプレッド相当)、remoteModels (local generated catalog が remote lastModified 以上なら overlay 抑制)、4時間 freshness gate (force でバイパス)、refresh 判定ツリー (304 は freshness window のみ移動し etag 保持、404/501 は validator クリア + lastModified=0、一時的失敗は etag 保持で upstream 文言エラー、成功は checkedAt/lastModified/etag 記録 + overlay publish)。+ parity 11テスト。divergence: HTTP fetch (fetchWithRetry) と provider トレイト結線は host 側、HTTP date パースは unix-ms のみ (RFC 1123 完全パースは host 側)。**model-registry facade 完了 (2fe000d)**: core/model_registry.rs (model-registry.ts 157行)。host 注入の ModelRuntimeFacade トレイト越しの同期ファサード: ResolvedRequestAuth の整形 (api key/headers/baseUrl/env)、解決なし時は compatibility request config へフォールバック (authHeader 要件時は `No API key found for <provider>` エラー)、"authHeader requires a resolved API key" cause メッセージのマップ、provider 表示名は生 id へフォールバック、AuthStatus 形。+ parity 7テスト。divergence: ModelRuntime 本体 (models.json refresh、auth 解決、provider completion) は未移植で host がトレイト経由で接続。**extras 小モジュール群完了 (f71569e)**: core/extras.rs — experimental.ts (PI_EXPERIMENTAL=1 フラグ + strict json_schema tool sampling ヒント)、telemetry.ts (PI_TELEMETRY env が enableInstallTelemetry 設定を override、共通 truthy env フラグパース)、radius.ts (RADIUS_PROVIDER_ID 定数)、output-guard.ts (stdout take-over ステートマシン: 冪等な take_over/restore、空 write は破棄、ENOBUFS/EAGAIN/EWOULDBLOCK は 10ms retry で再試行可能な backpressure エラー)。+ parity 8テスト。divergence: 生 stdout write の実装は TUI プロセスの関心事で host 側、ポートは take-over 状態と契約を追跡。**sdk session-assembly コア完了 (7ccaee0)**: core/sdk.rs (sdk.ts 410行の decision コア)。モデル復元 (session 保存の provider/modelId を host lookup + 認証設定ゲートで復元、"Could not restore model" フォールバック文言 + ". Using provider/id" 合成)、thinking level 解決チェーン (explicit → session entry → entry なし session は設定 default → per-model override → global default → DEFAULT_THINKING_LEVEL、モデル capability にクランプ、モデルなしは "off"、不明 level は default へフォールバック)、tool 選択 (明示 allowlist 優先、noTools all/builtin は空スタート、設定 defaultTools または組み込み read/bash/edit/write 既定、excludeTools は後段フィルタ)、convertToLlm block-images フィルタ (画像を "Image reading is disabled." プレースホルダへ置換し連続重複を dedupe)。+ parity 11テスト。divergence: ModelRuntime lookup と streaming fn 結線は host 注入、findInitialModel の provider default 探索は host 側。**agent-session utilities 完了 (64b233b)**: getLastAssistantText (最後の assistant を探索 — content が空の aborted はスキップ、text block を連結して trim、空なら None。/copy コマンド用)。+ parity 3テスト。divergence: exportToHtml/exportToJsonl の結線は既存の export_html と session_support の export_session_to_jsonl で賄う。**agent-session bash 実行フロー完了 (44115fd)**: BashSessionState (実行中カウント + pending メッセージキュー — ストリーミング中は tool_use/tool_result 順序を守るため遅延、turn 終了後に flush、非ストリーミングは即時 append。BashExecutionMessage は command/output/exitCode/cancelled/truncated/fullOutputPath/timestamp/excludeFromContext ("!!" prefix) をそのまま保持)、resolve_shell_command (設定された shell prefix を改行付きで前置)。+ parity 5テスト。divergence: プロセス spawn 自体は移植済み bash_executor (execute_bash_with_operations) が担当し本モジュールは session 側ステートマシンのみ。**agent-session tree navigation + context usage 完了 (d5e234c)**: plan_tree_navigation (user/custom message は leaf を親に移動し text を editor へ、他 entry は leaf が対象自身 — upstream navigateTree の判定コア)、getUserMessagesForForking (fork selector 用の非空 user entry 一覧)、compute_context_usage (context window 0 は None、compaction 後は post-compaction assistant の有効 usage が現れるまで tokens/percent 不明 — upstream の null セマンティクス)。+ parity 6テスト。divergence: 要約 LLM 呼び出し、session_before_tree 拡張フック、agent state の更新は host 側 (判定ロジックと usage 計算のみ移植)。**agent-session コア完了 (9e87f90)**: core/agent_session.rs (agent-session.ts 3516行の session-state コア)。parseSkillBlock (skill XML ブロック + 末尾 user message) と _expandSkillCommand (ブロック名を prompt template 呼び出しに変換、user message が引数)、retry 設定ステートマシン (will_retry_after_agent_end は最後の assistant メッセージを判定、is_retryable_error は context overflow を除外、prepare_retry は指数バックオフ + 予算枯渇)、sendCustomMessage の場合分け (nextTurn/steer/followUp/triggerTurn/turn-end 遅延/即時 append)、steering/follow-up キュー追跡 (message_start 時に steering 優先で除去)、tool registry (未知名フィルタ、snippet 1行化、guideline dedupe)、/session メッセージ集計。+ parity 12テスト。divergence: モデル切替/サイクルは未移植の ModelRuntime 依存で host 注入、ストリーミング実行ループと compaction オーケストレーションは host 側、Agent へのキュースナップショットは未露出。**extensions loader 完了 (b648219)**: core/extensions_loader.rs (extensions/loader.ts 806行の runtime-state + discovery コア)。ExtensionRuntime (bind 前は "not initialized" エラーの stub、bind_action/call_action、初回固定の stale invalidation + 既定文言、tracked event-bus unsubscription は冪等)、provider 登録の queue/drain/unqueue (bindCore flush 相当)、拡張パス探索 (.ts/.js ファイル + package.json pi manifest / index.ts/index.js サブディレクトリ展開)、discoverAndLoadExtensions の順序 (project .pi/extensions → agent extensions/ → configured、canonical path dedupe)、cwd + generation キーのモジュールキャッシュ (clear() で generation 増加、cwd 変更でクリア、パス毎エラー収集)。+ parity 12テスト。divergence: TS/JS モジュールのロードは JS ランタイム依存のため loader は host 注入コールバック、キャッシュは host 提供の拡張値を保持。**extensions runner 完了 (9918ef4)**: core/extensions_runner.rs (extensions/runner.ts 1286行の実行コア)。buildBuiltinKeybindings (key 正規化、reserved action が反復順序に関係なく勝つ)、ショートカット解決 (reserved は Skipping 診断、非 reserved は上書き警告、拡張間競合は後勝ち — 文言完全一致)、コマンド invocation 名 dedupe (重複名は `name:1`/`name:2`、リテラル衝突は suffix スキップ — テストで upstream が最初の出現にも数字を付けることを確認)、tool/flag の first-wins レジストリ、emit (session_before_* は cancel:true で短絡、ハンドラエラーは listener 通知で継続、非 session-before は undefined を返す)、emitToolCall (block 短絡 + エラーは伝播)、emitToolResult (フィールド連鎖、未変更は undefined)、emitMessageEnd (role 変更拒否)、emitInput (transform/handled 連鎖)、emitContext (一括置換)、emitBeforeAgentStart (system prompt 上書き + message 蓄積)、emitResourcesDiscover、project_trust (最初の決定が勝ち undecided は fall-through)、session_shutdown はハンドラ存在時のみ発火、invalidate は初回メッセージ固定。+ parity 20テスト。divergence: ハンドラは JSON event payload 上の host 提供クロージャ (JS Extension ランタイム/TUI レンダラ未移植)、context オブジェクトは call-time getter に縮約、provider 登録はコールバック。**agent-session-services + agent-session-runtime 完了 (a8365c5)**: core/agent_session_runtime.rs (agent-session-services.ts 221行 + agent-session-runtime.ts 441行)。createAgentSessionServices (cwd バウンドの settings/resource-loader 束ね、settings 診断を warning として収集)。AgentSessionRuntime: switchSession (stored-cwd 検証 + cwd override 付き resume)、newSession (persisted/in-memory 分岐 + parent session リンク)、fork (at/before 位置 — before は選択 entry 自身の user text を返す、persisted は createBranchedSession、未保存エラー文言、in-memory は branch/new session)、importFromJsonl (ファイル未存在は SessionImportFileNotFoundError、session dir へコピー + resume)。+ parity 13テスト。divergence: 拡張イベント (session_before_switch/before_fork/shutdown) は JS ランタイム未移植のため caller 所有のキャンセル/通知フックに置換、ランタイムファクトリは SDK セッション組立の代わりの caller 提供クロージャ (ModelRuntime は未着手)。**resource-loader 完了 (0d9ffe1)**: core/resource_loader.rs (resource-loader.ts 1097行)。loadProjectContextFiles (AGENTS.override.md/AGENTS.md/AGENTS.MD/CLAUDE.md/CLAUDE.MD 候補チェーン、global agent-dir ファイル先頭、祖先ファイルは近い順、worktree shadowing — findGitPaths が gitdir:/commondir を解決し main worktree root が同一リポジトリのチェックアウトか検証して main 側 context file を隠す)。DefaultResourceLoader: package-manager 解決パスの skills/prompts/themes へのマージ、CLI temporary パス、additional パス + 欠損診断、extendResources (metadata 由来の source info 解決、prefix マッチ)、auto/package リソースの SKILL.md ディレクトリマップ、root ディレクトリによる既定 source info (user/project/temporary)、prompt/theme 名前 dedupe + collision 診断、system prompt / append system prompt 探索 (.pi/SYSTEM.md、APPEND_SYSTEM.md、project trust ゲート)、拡張 tool/flag 名競合検出。+ parity 14テスト。divergence: 拡張ロードは JS ランタイム依存のため host 注入 (パスのみ追跡)、theme は生 JSON 保持 (theme レンダラ未移植)、no* フラグ対応。**session-support 小モジュール群完了 (461c454)**: core/session_support.rs — defaults.ts (DEFAULT_THINKING_LEVEL/THINKING_LEVEL_OPTIONS)、session-export.ts (exportSessionToJsonl: branch を parentId チェーンに再直列化、trailing export-only entries、ディレクトリ自動作成、タイムスタンプ既定ファイル名)、slash-commands.ts (SlashCommandInfo/BuiltinSlashCommand + BUILTIN_SLASH_COMMANDS 22個を文言完全一致で)、session-cwd.ts (保存 cwd 欠損検出 + error/prompt 文言 + assertSessionCwdExists)、settings-diagnostics.ts (drain → warning 診断 + type/message 重複排除は初出保持)。toISOString 形は chrono なしの civil_from_days 実装 (format_epoch_millis)。+ parity 9テスト。**package-manager 完了 (未コミット)**: core/package_manager.rs (package-manager.ts 2699行)。ソースパース: parseNpmSpec (名前に @ を含まない正規表現相当)、npm pinned/range 判定 (semver クレート)、parseGitUrl (git: プレフィックス or プロトコル URL のみ受理 — bare scp-like git@host:path は upstream 同様拒否、URL userinfo 剥き、.git 除去、unsafe path 検証) — hosted-git-info は非移植の divergence、isLocalPath、dedupePackages (project 勝ち、autoload=false delta は両方保持)、パターンフィルタ (include/!exclude/+force-include/-force-exclude の 4 段階、SKILL.md 親ディレクトリバリアント)、resourcePrecedenceRank (project-local 0 → package 4)、リソース収集 (pi/agents モード skill 探索、拡張 manifest/index.ts 探索、gitignore 風 ignore マッチャ、.gitignore/.ignore/.fdignore プレフィックス付き)、install path 計算 (npm managed/legacy global、git host/path レイアウト、hashed temporary dir、resolveManagedPath の .. 正規化 + root 外拒否)、install/remove/update ランナー (bun/pnpm/npm 別フラグ、git ref fetch/reset + marker + 依存修復、進捗イベント)、resolve (local top-level + auto discovery: .pi/ + .agents/skills 祖先 + ~/.pi/agent/ + ~/.agents/)。+ parity 38 テスト。divergence: 生成 diff は省略、コマンドランナーは std::process::Command (stdout take-over なし)、resolve は同期、sha256 は FNV-1a 64bit 16進に置換 (temp dir 名のみに使用)。**cache-stats 完了 (6cc1d02)**: core/cache_stats.rs (cache-stats.ts 164行) — プロンプトキャッシュ浪費の集計。missedTokens = min(prev.promptTokens, promptTokens) - cacheRead、ノイズフロア 1024 以下は無視、zero-cache ターンは prior に cache 報告があった時のみカウント (reportedCache sticky — cache-read-only provider の total miss と非対応 provider の区別)、compaction/branch_summary で scan リセット (model 切替はリセットされない)、missedCost = missedTokens x (このメッセージの paid rate − read rate、read 0 なら pricing source フォールバック)。モデル価格は ModelPriceSource トレイトに抽象 (upstream は AssistantMessage 直接参照 — ポートは使用フィールドに絞った CacheStatsMessage を定義し SessionEntry に依存しない)。+ parity 16テスト。**resolve-config-value 完了 (未コミット)**: core/resolve_config_value.rs (resolve-config-value.ts 287行) — config 値解決 (`!command` シェル実行 + プロセス生存キャッシュ、`$VAR`/`${VAR}` 補間、`$$`/`$!` エスケープ、リテラル)、env var 名ヘルパ (単一参照/一覧/欠損検出)、resolve_config_value_or_throw (コマンド/単一 env/複数 env のエラーメッセージ)、resolve_headers (+ or_throw、解決不能ヘッダは脱落)。+ parity 24テスト。divergence: コマンド実行は `sh -c` 単経路 (upstream は Windows で設定シェル + fallback、他は execSync — ポートは POSIX 想定); env 値は空文字を未設定扱い (upstream `||` セマンティクス)。**skills ディレクトリ走査完了 (未コミット)**: core/skills.rs (skills.ts 507行の走査部) — load_skills_from_dir (SKILL.md で停止する再帰、ルート直下 .md 読み込み、node_modules/dotfile スキップ)、load_skills (user agentDir/skills → project cwd/pi/skills → 明示パス、~ 展開、実パス重複排除、名前衝突は先勝ち + collision diagnostic)、SKILL.md YAML パーサを block scalar 対応に拡張 (| 改行結合 / > 空白結合 — 複数行 description)、未閉鎖 bracket/brace は "at line N" エラー、ignore パターン近似 (gitignore/ignore/fdignore、suffix/segment マッチ — `ignore` クレート不使用)、name 検証メッセージに文字数埋め込み。+ parity 22テスト。divergence: ignore マッチは近似 (upstream は ignore クレートの完全 gitignore セマンティクス); sourceInfo は host 側で管理。**model-config 完了 (未コミット)**: core/model_config.rs (model-config.ts 232行) — models.json の Immutable スナップショット。serde 構造バリデーション (typebox スキーマ相当: Model/Override/Provider/Compat 3系統/cost tiers)、JSONC 対応 (手書き stripJsonComments — 行コメント除去 + 末尾カンマ除去、文字列内は保護、lookahead 方式)、欠損ファイルは空 config、parse/schema エラーは error 文字列にパス付きで記録。+ parity 12テスト。divergence: typebox の union は宣言順で先勝ち & unknown field を無視するため、responses/anthropic 形の compat も completions variant に解決される (実害なし — compat は API 型経由で消費)。**pillar-tui クレート新設 + keybindings 完了 (未コミット)**: crates/pillar-tui — keys.rs (keys.ts 1300行の一致判定コア: legacy シーケンス / Kitty CSI u / xterm modifyOtherKeys / raw ctrl、 Kitty プロトコル active フラグはグローバル状態、raw backspace の WT 判定は env 参照 — テストでは process env 制御が必要) + keybindings.rs (TUI_KEYBINDINGS 45定義、user override は該当 binding のみ置換し他の default を退避しない、直接 user binding の key 衝突のみ検出)。coding-agent core/keybindings.rs — App 定義 42個 (Windows/WSL/darwin 分岐、win32 native は ctrl+z が windowsKeybindings の alt+z より優先 — spread 順)、legacy 名 migration (60名、新旧同名時は namespaced 側を残し migrated=true)、keybindings.json 読み込み (非文字列エントリは破棄)、reload/create。+ parity 13テスト (tui 9 + coding-agent keybindings 9 / migration 3 / windows 5)。divergence: 上流は宣言マージで Keybindings 型を拡張するがポートは BTreeMap 定義; editor-history 優先は registry ではなく editor コンポーネント側の責務。**prompt-templates 完了 (d8eb159)**: core/prompt_templates.rs (上流 prompt-templates.ts 285行) — parse_command_args (bash 方式クォート対応)、substitute_args ($1/$2, $@/$ARGUMENTS, ${N:-default}, ${@:N}, ${@:N:L} bash 式スライス、再帰置換なし)、load_prompt_templates (agentDir/prompts + cwd/pi/prompts + 明示パス、include_defaults 制御)、expand_prompt_template (/name args 展開、未一致は原文)。frontmatter は最小行パーサ (description/argument-hint、description 未設定時は最初の非空行を60文字で切り詰め「...」)。+ parity 19テスト。divergence: yaml パッケージ不使用 (消費キーはスカラーのみ); 上流テストは vitest fixture、ポートは temp_dir。**pillar-coding-agent クレート新設 + system-prompt 完了 (未コミット)**: `crates/pillar-coding-agent` を workspace に追加。core/system_prompt.rs (system-prompt.ts 移植, ~350行) — build_system_prompt (デフォルト/カスタムプロンプト、ツールスニペット、guideline 重複排除、project_context 追加、skills XML セクション)、format_skills_for_prompt (agentskills.io XML、disable_model_invocation 除外)、SKILL.md frontmatter パーサ + name/description 検証 (Agent Skills spec)。+ parity 16テスト全パス (system-prompt.test.ts 相当)。divergence: pi docs パスは PromptPaths として host 注入 (上流は package dir 解決); skills のディレクトリ走査 (ignore パターン付き) は未移植 — frontmatter 検証とフォーマットのみ。**models catalog ジェネレータ + builtin providers 完了 (未コミット)**: `src/bin/generate-models.rs` (上流 generate-models.ts の移植) — models.dev api.json + OpenRouter /v1/models + Vercel AI Gateway /models を取得し、compat 検出 / thinking-level map / 価格 override / static catalog (codex, deepseek-v4, ant-ling, azure clone 等) を適用して `src/models_generated.rs` (39 providers / 1371 モデル) を生成。`src/models_catalog.rs` が JSON を Model に変換。`src/providers_all.rs` が builtin provider 登録 (auth 定義: envApiKeyAuth / Cloudflare account+gateway / Bedrock cred chain / Vertex ADC / Radius placeholder) と `builtin_models()` を提供。+ parity 4テスト。divergence: 上流は data/*.json (gitignored) + .models.ts shard を出すが、ポートは単一 generated rs に集約; stream dispatch も `src/api_dispatch.rs` で結線済み (StreamRequestOptions → 各アダプタ Options の From 変換を 10 API に実装、google_vertex.rs は未登録だったので api/mod.rs に登録して修復)。catalog/auth/stream の 3 層がすべて生きた。**images-models 完了 (未コミット)**: images_models.rs — ImagesModels / ImagesProvider / create_images_provider (in-flight refresh dedupe via oneshot slot)。auth 解決は共有 resolve_provider_auth、options との per-field マージ (apiKey は explicit 優先 / headers・env は key マージ)、未設定 auth でもディスパッチ継続、不明 provider は error 結果。+ parity 7テスト全パス。**images 完了 (未コミット)**: openrouter_images.rs — images 型群 (ImagesModel/Context/AssistantImages/StopReason/Options + ImagesOnPayloadFn/ImagesOnResponseFn) と openrouter-images アダプタ (唯一の images API)。chat/completions 非ストリーミング POST、modalities (text 出力対応で [image,text])、data: URL パース (prefix チェックのみ, MIME allow-list なし — 上流同様)、object 形 image_url 対応、usage の cached/cache_write 差引計算、onPayload/onResponse フック。+ parity 9テスト全パス (live E2E images.test.ts は OPENROUTER_API_KEY 依存で非移植)。**pi-messages 完了 (4a9b015)**: pi_messages.rs (~900行) — pi 独自ワイヤプロトコル (Radius ゲートウェイが話すもの)。POST {model, context, options} を `<baseUrl>/messages` へ送り、SSE でシリアライズ済み assistant-message イベントを受信、terminal done/error で終了。イベントコンバータ (text/thinking/toolcall start/delta/end、スパース slot 補間)、toolcall の streaming-JSON パース、rewrite impact 診断、debug=1 クエリ、PI_CACHE_RETENTION レガシー env、非2xx は error-body パース + pi_messages_response_failure 診断。+ parity 6テスト。ターミナル done の reason は文字列 (stop/length/toolUse) で来るので StopReason 変換が必須 (初期バグ)。`cargo fmt --check` / `cargo clippy` (クレート毎に `-D warnings`) クリーン。**azure-openai-responses 完了 (未コミット)**: azure_openai_responses.rs (~700行) + azure_openai_responses_parity.rs 24テスト全パス。Azure URL 正規化 (/openai/v1 への書き換え, 非Azure プロキシの保持, クエリ除去)、AZURE_OPENAI_BASE_URL / RESOURCE_NAME / API_VERSION / DEPLOYMENT_NAME_MAP 解決 (scoped env 注入)、deployment name フォールバック、store:false、prompt_cache_key クランプ、strict-mode compat、pi User-Agent + ヘッダ上書き、tool_choice 転送、reasoning encrypted_content バックフィル (terminal response から output_item.done 欠落分を補完)。divergence: `openai` npm SDK (AzureOpenAI クライアント) を使わず生リクエスト + `?api-version=` クエリ; `url` クレート不使用の手動パース (bedrock と同方針)。**openai-codex-responses 完了 (60759ac)**: openai_codex_responses.rs (2.5k行) + openai_codex_stream_parity.rs 20テスト全パス (SSE 15 / WebSocket 5)。SSE トランスポート (zstd 圧縮 #zstd crate, Codex ヘッダ, リトライ/指数バックオフ, friendly usage-limit メッセージ, service tier 料金 flex 0.5x/priority 2x/2.5x, accountId JWT 抽出, cache key/session-id 64文字クランプ) + WebSocket トランスポート (WsConn/WsConnFactory トレイト注入 — 上流は global WebSocket をモック, 接続プール per (sessionId, accountId), previous_response_id 継続, SSE フォールバック, 接続タイムアウト, idle タイムアウト)。divergence: strict: null は共有ヘルパーの Option<bool> で表現できないため Codex アダプタで false→null リマップ; Bun プロキシ WebSocket 非該当; Date.parse(retry-after) フォールバック省略。**bedrock-converse 完了 (cc1eac9)**: bedrock_converse_stream.rs (2.3k行, AWS SDK不使用 — FetchFn + 生HTTP + AWS eventstream バイナリフレーミングデコーダ #54) + bedrock_converse_stream_parity.rs 40テスト全パス。テスト修正過程で判明した落とし穴: テストヘルパーの `assistant_message()` が model ID を base_model にハードコードすると `transform_messages` の `isSameModel` 判定が true になり tool call ID 正規化・redacted thinking 保持が発火しない (#58)、struct update 構文のデフォルト上書き (#57)、ambient AWS_PROFILE は process env 制御が必要 (#55)。**google 系 API 完了 (9717334)**: google-shared.rs + google-generative-ai.rs + google-vertex.rs + 27テスト。**mistral-conversations.rs 済み (ab4e80a + 300d7f9)**: SSE パーサ (境界はバイト検索 #53)。harness session の jsonl バックエンド済み (91a03b0 + f20fe8e) + jsonl パリティテスト 3スイート (#45/#47 バグ修正, #46 destination 予約, #47 spawn_blocking+inline runtime)。proxy.ts 済み (6ed4f6c)。search/ 済み (187bcf2)。e2e 済み (e1c55c1)。telemetry.ts スパン開始部完了 (fa2fe02)。

移植メモ (jsonl): `FileSystem` は RPITIT で dyn 非対応のため `JsonlSessionStorage<FT: FileSystem + ?Sized>` / `JsonlSessionRepo<F: FileSystem + 'static>` はジェネリクスで受ける (docs/INSTRUCTIONS.md #41)。書き込みは `SessionStorage` トレイトの sync メソッド内で `spawn_blocking` + インライン current_thread ランタイムにより append を駆動し、state mutex で直列化 (#47)。torn-tail 修復は `publishFileAtomically` (tmp + rename) を再現。

| クレート | テスト | 状況 |
| --- | --- | --- |
| pillar-protocol | 49 | ✅ 完了 |
| pillar-telemetry | 15 | ✅ 完了 |
| pillar-ai | 345 | 🔶 コア + 全プロバイダ + カタログ + builtin registry (core 35 / faux 22 / models-runtime 39 / api-infra 27 / openai-completions 23 / openai-responses 14 / anthropic-messages 27 / uuid 1 / google-shared 27 / mistral 20 / bedrock 40 / openai-codex 20 / azure 24 / openrouter-images 9 / pi-messages 6 / images-models 7 / providers-all 4) |
| pillar-agent | 299 | ✅ コアループ + Agent クラス + harness 基盤〜tools + telemetry + session jsonl バックエンド + jsonl パリティテスト + proxy + search + e2e 済み (lib 46 / loop 25 / agent 22 / nodejs-env 25 / utils 10 / skills 8 / messages 13 / session 20 / compaction 15 / reducer 28 / agent-harness-scaffold 4 / tools-parity 21 / proxy 1 / jsonl codec 14 / jsonl storage 5 / jsonl conformance 30 / search 4 / e2e 10) |
| pillar-coding-agent / tui / client / server / session-store | — | ❌ 未着手 |
| pillar-extensions | — | ❌ 未着手 (luaur VM 統合)。設計は docs/rules/04 に確定済み |

移植済みの主な到達点: agent ループ (agent-loop.test.ts 23ケース完全パリティ), Agent クラス, harness 基盤 (types/truncate/system-prompt/events/prompt-templates/env/shell-output/skills/messages), compaction 本体 + 共通部 + branch-summarization 準備部, session v4 ツリー (types/state/context/memory = InMemory バックエンド), result.ts (TaggedErrorValue), reducer.ts (validateRecordLog/reduceLaneState), agent-harness.ts (スキャフォールド: create/設定 getter-setter/未実装操作の明示拒否), tools/ (bash/edit/read/write + path-utils/image/file-mutation-queue/edit-diff)。

## 全体進捗の目安 (2026-09-02 時点の行数実測)

上流パッケージ規模 (src = テスト以外の .ts, `/tmp/upstream/pi` 実測)。上流合計 約106k行:

| パッケージ | 上流 src | pillar 状況 |
| --- | --- | --- |
| protocol | 1.2k | ✅ 完了 (rs 2.5k / 49テスト) |
| telemetry | 0.9k | ✅ 完了 (rs 0.7k / 15テスト) |
| ai | 23.6k | 🔶 **移植対象スコープ比 約85–90%** (rs 37.1k / 289テスト)。api/ 11.7k + providers/ 2.4k + root/auth コア + カタログ済み。残りは live-API 専用の oauth (3.0k) / Cloudflare binding / cli のみ |
| agent | 12.7k | ✅ 実質完了 (rs 19.5k / 285テスト)。残りは live-API 依存 e2e のみ |
| coding-agent | 49.4k | 🔶 **約33%** (core 29.5k のうち約55%移植済み, rs 17.5k / 537テスト)。残り: agent-session 4.2k / extensions ランナー 4.1k / resource-loader 1.1k / sdk 0.4k / core/tools bash+powershell 0.6k / cli 1.9k / utils 3.6k / modes 20.3k |
| tui | 17.0k | 🔶 約8% (keys + keybindings, rs 1.2k / 9テスト)。残り: components 9.9k + interactive-mode 6.9k + theme 2.0k |
| server / client / session-store | 5.0k | ❌ 未着手 (server 2.3k / client 1.2k / sqlite 1.5k) |
| evals | 1.3k | ❌ 対象外 |

**全体: 行数比で約4割強 (移植対象スコープ換算 約42%)。** 前回集計 (2026-08-31, 体感 2割強) から coding-agent の core 移植が大きく進んだ。土台層 (protocol / telemetry / ai / agent) は最難関部 (SSE パーサ・非同期セマンティクス・イベント順序の厳密互換) を含めて固まり、1,115テストで保護済み。残り約6割は coding-agent の modes/tui (UI 系 38k) と coding-agent 残り core (agent-session/package-manager/extensions)。UI 系は土台の上に載せる形なので行数比よりは速く進む見込みだが、agent-session と extensions ランナーは結合部が多く難所。

## 作業指示チェックリスト

ユーザー指示を記録し、進捗に合わせて更新する。

- [x] luaur PR #40 のレビュー反映 (2026-09-02)
  - [x] PR 本文を現行実装・テスト結果に合わせて更新する (tuple fast path / serde 回帰テスト / 253 tests)
  - [x] serde 回帰テストの `map_err(|e| e)?` を単純な `?` に整理し、commit `2ae9ea74` を push (2026-09-02)
  - [x] 検証: 対象 serde 回帰テスト・`cargo fmt -p luaur-rt --check` パス。push 後 CI 6ジョブ全パス
  - [x] Confirm (user): PR 上の最終内容の承認 — user-confirmed (2026-09-02)

- [x] luaur follow-up PR #40 の修正後レビュー (2026-09-02)
  - [x] commit `37636ee5` 以降の実装・テスト・PR 本文を再確認する (2026-09-02)
    - 実装: tuple の stack fast path は正しく伝播。3-tuple・nested tuple・戻り値不足を追加プローブして全パス
    - テスト: Rust→Lua→Rust、mixed tuple、serde/JSON 回帰ケースが追加され、初回指摘は解消
    - CI: Ubuntu / Windows / macOS / fmt / wasm / clean-check の全ジョブ成功
    - 残件: PR 本文が更新前のまま。`252 passed`→`253 passed`、serde の「verified manually」→コミット済み回帰テスト、tuple fast path の説明追加が必要。`map_err(|e| e)?` は単純な `?` にできる軽微な nit
  - [x] Confirm (user): 修正後レビュー結果の承認 — user-confirmed (2026-09-02)

- [x] luaur follow-up PR #40 のレビュー (2026-09-02) — **指摘4点すべて修正済み・push済み (37636ee5)**
  - [x] PR 本文・実装・PR #37 コメントを確認し、異議と修正文案を提示する (2026-09-02)
    - 要修正: tuple の `FromLuaMulti` が `from_stack_multi` を override しておらず、`eval::<(f64,)>()` 等では引き続き `-0.0` の符号を失う (mlua の tuple 実装は stack fast path を伝播する)。再現テストで確認 → **修正**: `impl_tuple_from` の各アリティに `from_stack_multi` を override 追加 (各 `FromLua` スロットはスタック直読み、1-tuple は全域転送)。`(f64,)` / `(f64,f64)` / `(i32,f64)` プローブで符号保持を確認
    - テスト記述: Rust→Lua→Rust の素通しケースが実装されておらず、同一 assertion が重複。PR 本文の説明とも不一致 → **修正**: 重複 assertion 削除し、`call::<(f64,f64)>(-0.0)` + mixed `(i32,f64)` tuple の Rust→Lua→Rust 素通しに書き換え
    - 添削: f32 の value-level conversion error は従来の `to: f64` から `to: f32` に変わるため「error messages unchanged」は不正確。serde 回帰ケースはコミット済みテスト化を推奨 → **修正**: f32 は `f64::from_lua` 委託に戻しエラーメッセージを従来通り維持、serde 回帰ケースを `mlua_serde.rs` に `test_from_value_whole_number_floats_stay_integers` として追加 (JSON 形状検査込み)
    - PR #37 コメント自体は適切。#40 が後継であるため #37 は close 推奨 → maintainer の判断待ち (自分では close しない)
  - 検証: luaur-rt 253 tests / mlua_serde 21 tests (serde feature) / workspace 5725 tests 全パス、fmt クリーン、clippy luaur-rt 内エラーなし
  - [x] Confirm (user): 修正内容の承認 — user-confirmed (2026-09-02)

- [x] 考察: 今後の独自性としての「Rust製エージェントランタイム + id_effect」の相性と方向性 (2026-09-02) — user-confirmed (2026-09-02)
  - 結論: 外部境界は厳密互換を維持し、実行モデル (プロバイダ層/ツール実行層/拡張ランタイム) への Effect セマンティクス導入を独自性の中核にする。id_effect / id_effect_ai は pillar から独立した leaf として育成
  - 移行は 3段階: (1) 移植完了後、id_effect でプロバイダ層を対照実験的に再実装 (#53/#49/#3/#4 のバグクラスが消えるか既存 parity テストで検証) → (2) 新規コンポーネント (pillar-extensions / server / session-store / agent-harness 操作本体) を Effect ベースで → (3) コア (agent-loop) は外部 API 互換のまま内部 Runtime を吸収
  - **ユーザー確認済み: 完全移植完了後の道筋であり、移植完了前には実装に着手しない** (対照実験含む)。移植を最優先する
- [ ] 「docs/INSTRUCTIONS.md から引き継いで続きを頼む」— 次の作業キューに従って移植を継続する (2026-08-31)
  - [x] pillar-ai: google 系 (google-shared / google-generative-ai / google-vertex / providers/google*) — 9717334, 27テスト (9717334)
  - [x] pillar-ai: mistral-conversations — ab4e80a + 870fca8 (adapter + 20 parity tests)
  - [x] pillar-ai: bedrock-converse — アダプタ完成 + パリティテスト 40ケース全パス (2026-09-01)。AWS SDK 未使用, FetchFn + 生 HTTP + AWS eventstream (vnd.amazon.eventstream) バイナリフレーミングデコーダで構築 (google-vertex #52 同型 divergence: SigV4 サインクリック対策なし, bearer token / skip-auth 経路のみ自己完結)。初期6失敗はすべてテスト側のバグだった: ヘルパーの model ハードコード (#58), struct update のデフォルト上書き (#57), ambient profile の process env 制御 (#55), capture_payload の cache_retention 強制 — 実装の修正は不要だった。clippy: ptr_arg/type_complexity/未使用ヘルパー整理済み
  - [x] pillar-ai: openai-codex — アダプタ (2.5k行) + パリティテスト 20ケース全パス (2026-09-01, 未コミット)。SSE (zstd 圧縮, Codex ヘッダ, リトライ, usage-limit friendly メッセージ, service tier 料金, JWT accountId 抽出, 64文字クランプ) + WebSocket (WsConn/WsConnFactory トレイト注入 — 上流は global WebSocket モック; 接続プール per (sessionId, accountId), previous_response_id 継続, SSE フォールバック, connect/idle タイムアウト)。divergence: strict:null はアダプタ側で false→null リマップ; Date.parse(retry-after) 省略; Bun プロキシ非該当。live-API (oauth 486行, cache-affinity e2e) は非移植
  - [x] pillar-ai: azure — アダプタ (~700行) + パリティテスト 24ケース全パス (2026-09-01, 未コミット)。URL 正規化 / env 解決 / deployment 名 / strict-mode / tool_choice / reasoning replay。divergence: npm SDK を使わず生リクエスト + api-version クエリ; 手動 URL パース
  - [x] pillar-ai: images — openrouter-images アダプタ + images 型群 + parity 9テスト全パス (2026-09-01, 未コミット)。data: URL / modalities / usage 計算 / abort・error 結路。live E2E は非移植
  - [x] pillar-ai: pi-messages — アダプタ (~900行) + parity 6テスト全パス (4a9b015)。pi 独自ワイヤプロトコル (Radius ゲートウェイ)。ターミナル reason は文字列で来るので StopReason 変換が必須
  - [x] pillar-ai: images-models — ImagesModels コレクション + create_images_provider + parity 7テスト全パス (2026-09-01, 未コミット)。in-flight refresh dedupe は oneshot slot 方式 (上流は共有 Promise)
  - [x] pillar-ai: providers/all + generate-models — カタログジェネレータ (39 providers / 1371 モデル) + builtin provider 登録 + parity 4テスト (2026-09-01, 未コミット)。stream dispatch の per-API option 変換は未結線 (pending stub)
  - [ ] pillar-ai: providers/*
- [ ] luaur 上流 PR への返答対応 (2026-09-01)
  - [x] 状況確認: PR #38 (__index meta-method on fieldless userdata) と #39 (Table raw operations の stack slot 予約) はマージ済み、#37 (-0.0 の符号ビット保持) は CHANGES_REQUESTED で保留
  - [x] maintainer レビューの検証: (1) mlua 0.10.5 の `stack_value` は -0.0 を `Value::Integer(0)` に折り畳むため Value 層では 当PRの DEVIATION 記述は誤り、(2) 差分は `is_exact_integer(n) && n != 0.0` のため **plain 0 も Number(0.0) になり serde (deserialize_any → visit_f64) と JSON 形状を壊す** — どちらも指摘通り。真の修正層は f64/f32 の from_stack fast path (luaur の呼び出し経路は exec_raw が一旦 `Vec<Value>` に materialize してから `FromLuaMulti` するので、値を Value 経由で復元する限り -0.0 の符号は失われている)
  - [ ] 返答ドラフト提示・送信 — 送信済み (2026-09-01)。serde リグレッションの承認 + follow-up PR (#40) の案内をコメントした。<https://github.com/pjankiewicz/luaur/pull/37#issuecomment-5502370281>
  - Confirm (user): 返答文の承認 — 承認不要で follow-up 対応の一環としてユーザー指示で送信、内容は PR 作業で実施 (2026-09-01)
- [x] luaur PR #37 への follow-up PR: f32/f64 の from_stack fast path (2026-09-01) — **PR #40 作成済み** <https://github.com/pjankiewicz/luaur/pull/40>
  - 実装: `FromLua` に `#[doc(hidden)] unsafe fn from_stack(idx, lua)` (default は value_from_stack → from_lua)、`FromLuaMulti` に `from_stack_multi(base, nvals, lua)` (単一値 blanket impl は from_stack に転送)、`f64/f32` が `lua_tonumberx` 直読みを override、`Function::call` / `Lua::exec_raw` の結果回収を `R::from_stack_multi` 経由に変更 (ここで符号が失われていた)。`value_from_stack` の整数正規化は変更せず serde 無傷
  - 検証: luaur-rt 252 tests / workspace 5725 tests 全パス (以前の 22 失敗はバイナリ未ビルドが原因で `cargo build --workspace` 後はパス)、serde regression ケース (Cfg{count:0,idx:0,ratio:0}) --features serde でパス、`cargo fmt --check` クリーン、clippy は luaur-rt 内エラーなし (luaur-common の既存エラーは CI ゲート外)
  - 落とし穴メモ: luaur VM 自体は `-0.0` を保持しており、符号喪失の実体は exec_raw/function.rs が `Value` を経由して戻り値を materialize する箇所にあった。mlua 同様の from_stack フックが正しい修正層。fork (vi2q/luaur) の main は上流に3コミット遅れていたため branch は上流 main ベースで作成
  - Confirm (user): PR 本文と送信の承認 — 承認付きで送信済み (2026-09-01, ユーザー指示「修正を加えて欲しい」に基づき実施)

## 次の作業キュー

1. **agent の残り**: search/ (187bcf2) / e2e のモック可能部 (e1c55c1) は**完了**。残りは live-API 依存の e2e (responseid, xhigh, tool-call-without-result, tool-call-id-normalization — 非移植) と models_generated.rs (generate-models ジェネレータ, live カタログ依存) のみで、**agent クレートは実質完了**。agent-harness の操作本体 (prompt/compact/resume/watch 等) の依存先は揃ったので次は着手可能。→ 次: pillar-ai のプロバイダ残り または pillar-coding-agent 着手。
2. **pillar-ai**: **完了**。カタログジェネレータ (39 providers / 1371 モデル) + builtin provider 登録 + stream dispatch 結線済み。`cargo run -p pillar-ai --bin generate-models` で `models_generated.rs` を再生成できる。残るは live-API 依存の検証のみ (非移植方針)。→ 次: pillar-coding-agent 着手。
3. **pillar-coding-agent**: 着手。小規模 core 群 + auth-storage/models-store + model-runtime/runtime-credentials + messages + compaction + session-entries/branch-summarization + session-manager + settings-manager + trust-manager/pi-manifest + export-html + usage-totals 群 + truncate/strip-ansi/sanitize + bash-executor/exec + tools (output-accumulator/path-utils/render-utils/file-mutation-queue/ls/search/read/write/edit-diff/edit) + package-manager 完了。find/grep は FFF 設計参考のネイティブ実装に置換済み。package-manager (2.7k行) + session-support 小モジュール群 + resource-loader (1.1k行) + agent-session runtime/services 部 + agent-session コア + extensions runner・loader 完了。残り: agent-session 本体の結合部 (モデル切替/compaction オーケストレーション/ストリーミングループ) / extensions types (残部)。
4. **pillar-tui / client / server / session-store**: pillar-tui の keys + keybindings + fuzzy 完了。**pillar-session-store canonical storage SQL 層完了 (f56642b)**: storage.rs (storage/{entries,lanes,facts,records,writer-leases,session-sequences,session-stats,sessions}.ts 633行)。entry rows + filter scan、lanes (**leaf pointer 検証 'Lane N points at missing entry M'** + move 履歴 + operation claim 'already has an open operation')、facts (**NULL key は IS match — key IS ?**、label は key 毎の最新非 NULL)、lane records (open-operation は lane と type を検証)、**fencing writer leases (条件付き upsert は期限切れ holder からのみ fence を +1、renew は現 owner/fence かつ未期限のみ)**、sequences、stats 累積、session rows は最新 name fact と LEFT JOIN。+ parity 7テスト。divergence: INDEXED BY hint は削除 (SQLite が同等 plan を選択)、addUsageToStats は component field 受け取り、assertJsonSerializable は serde_json round-trip。テストで確認した upstream 挙動: 期限切れ lease は fence を bump して奪取可能、旧 owner は renew/release 不可、dangling lane pointer は失敗、NULL fact key は NULL のみ一致、session 名は最新 kind='name' key-NULL row から decode。**pillar-session-store SqliteSessionStorage + SqliteSessionRepository 完了 (2c0cd1d) — repo.ts 953行の移植で session-store 移植完了**。SqliteSessionStorage は pillar-agent の SessionStorage trait を実装 (appendEntry は lane head 親 + seq/timestamp 割当 + branch-cache 更新 + message-count stat、appendRecord は operation_started が lane を claim / operation_finished が解除 / usage が stats 累積、findEntriesOnBranch は boundary-aware cache query 上の cursor/typed filter、getLog は entry/record/lane/fact を seq 順に merge、name/label fact は JSON 値)。SqliteSessionRepository は create (row+sequence+stats+main lane+lease を 1 transaction で)、open (lease claim、active writer は拒否)、list (lease 不要の catalog)、delete (派生行を順に削除、欠損 session は no-op)、fork (tree scope は全 copy、branch scope は main lane branch を at/before 位置 semantics で copy、非 message target は拒否)、repairBranchCache、close。+ parity 10テスト。divergence: async SerialOperationQueue と heartbeat timer は共有 Mutex + 書き込み毎の lazy lease renewal (全 write は transaction 内で ownership 検証 — upstream と同等の保証)、Connection は Arc<Mutex<Option<Connection>>> で共有し onRelease callback で active-session 管理。テストで確認した upstream 挙動: seq は entries/records/lane-moves/facts で共有、重複 id は already_exists、fork 'before' は target の親まで copy、branch-fork target は cached branch 上の message entry が必須。次の候補: server testing harness (testing/*.ts)。
5. **pillar-extensions**: luaur VM 統合。設計は docs/rules/04。

## 移植の落とし穴

各項目の `#N` が安定番号。コードコメントからは `docs/INSTRUCTIONS.md #N` の形式で参照する。`(旧 #N)` は HANDOFF.md 時代の番号。

### 非同期・状態管理

- **MutexGuard を await またぎで保持しない** (#1, 旧 #1) — `std::sync::Mutex` のガードを async ブロック内で await またぎに保持するとコンパイルは通るが future が Send でなくなるか、同一ステートメント内の複数 `.lock()` で即デッドロックする。パターン: データは clone してから await、ガードは明示スコープで落とす。**sync 関数でも同じ**: ガードを保持したまま同じ mutex を再ロックする関数を呼ぶとデッドロックする (実例: `Models::clear_providers`)。テストが panic で途中止まりすると後段のデッドロックが隠れる (直したら別のバグが出る) ので注意。
- **EventStream::result() は複数回呼べること** (#2, 旧 #2, 修正済み: b04c4ce) — pi の Promise セマンティクスでは同じ promise を何度でも await できる。`Option::take()` で消費しないこと。
- **tokio watch の `send` は受信者がいないと値を捨てる** (#3, 旧 #10) — 全 Receiver が drop されていると `Err` を返し**値を保存しない**。abort フラグのように「後から subscribe する人も値を見る」べきものは `send_replace` を使う。実例: `AbortSignal::abort` — abort が待機タスクの subscribe より先に起きると `send` では何も起きず、後から来たタスクが永遠に待つ (models-runtime パリティテストのデッドロックの直接原因)。
- **`AbortSignal::any` は転送タスクを介さず同期的に合成する** (#4, 旧 #11) — 上流 `AbortSignal.any` はリスナーを同期的に attach する。tokio watch の受信を spawn した転送タスクで中継すると (a) subscribe 前の abort を取りこぼし、(b) `send` なら受信者不在で失敗し、(c) 呼び出し側が転送タスクのスケジューリングを待たずに `is_aborted()` を見るためレースする。現実装は composite が入力の `Receiver` を保持し、`is_aborted`/`reason`/`aborted_or_pending` が同期的に inputs を見る。
- **JS の promise は即座に走り、Rust の future は lazy** (#5, 旧 #12) — 上流テストの `const p = models.refresh(...); await started;` は promise が即実行される前提。Rust では `pending` を poll しないと provider コールバックが走らないので、`Box::pin` + `tokio::select!` で「started シグナルまで並行ドライブ」する必要がある (models_runtime_parity の3テストで修正済み)。
- **refresh の2フェーズ呼び出しで one-shot スロットを消費しない** (#6, 旧 #13) — `run_provider_operation` は refresh fn を2回呼ぶ (phase 1: allowNetwork=false, phase 2: true)。クロージャの呼び出し時に `Mutex<Option<Sender>>::take()` すると phase 1 が消費して phase 2 で `None` になる。テスト側は `Arc<Mutex<Option<_>>>` にして**使用時点** (allowNetwork チェック後) に take する。上流のクロージャ変数は永続するのが正。
- **spawn タスク内の panic は catch_unwind しないとプロセスを落とす** (#7, 旧 #20) — `tokio::spawn` 内で stream fn が panic すると、JoinHandle を無視しているので panic が外に伝播してテストプロセスごと死ぬ (旧 #3 の「panic が握り潰される」の逆 — 観測される前に死ぬ)。`stream_assistant_response` で `futures::FutureExt::catch_unwind` + `AssertUnwindSafe` で包み、panic を stopReason "error" + errorMessage の AssistantMessage に変換した (上流 handleRunFailure 相当)。
- **Agent の待ち合わせ構造** (#8, 旧 #20) — 上流は `emit()` をループ内で await するが、Rust ループは spawn で stream に push するだけ。Agent は `drive_agent_stream` で EventIter を消費しながら `process_event` (状態 reduce + リスナー await) を同期的に回す。リスナーが完了するまで prompt が返らない保証はこの構造で出している。
- **ListenerEntry の take/push パターン** (#9, 旧 #20) — リスナー dispatch 中に再 lock しないよう、`std::mem::take` でリストを抜いてから順に await し、各リスナーを呼び終えたら push し戻す。簡易だが「リスナー内部で subscribe/unsubscribe」も壊れない。
- **session_id 等の可変フィールドは `Arc<Mutex<Option<String>>>`** (#10, 旧 #20) — `Agent` は `&self` で使う (tokio::spawn に置くため Arc<Agent>)。setter は内部可変性で。
- **並列ツールの onUpdate はバッファ → settle 後 drain** (#11, 旧 #20) — 上流は updateEvents 配列に Promise を積み、`execute` 返却後に `Promise.all` で flush する。tokio::spawn で即配信すると実行順序とイベント順がずれ、late-update テスト (settle 後は無視) も壊れる。sync バッファ + `AtomicBool accepting` で再現。
- **フッククロージャは `&参照 -> Future` で借用が future に食い込む** (#12, 旧 #21) — `Box::pin(async move { ... context.field ... })` と参照を直接持ち込むと lifetime エラー。クロージャ本体で必要なデータを clone してから `Box::pin(async move ...)` に渡す。
- **`Arc::new(クロージャ)` を `Arc<ConvertToLlmFn>` 等のトレイトオブジェクト型に `.into()` できない** (#13, 旧 #21) — フィールド型が既に `Option<Arc<dyn Fn...>>` なら `Some(Arc::new(明示型注釈付きクロージャ))` を直接代入する。`|messages| {...}` だと推論が効かないので `|messages: &[AgentMessage]| -> Vec<Message>` と注釈する。
- **並列実行の可視化テストは `tokio::sync::Notify` がゲートに便利** (#14, 旧 #21) — 上流の `new Promise + setTimeout(release, 20)` は `Notify::notify_one` + `release_gate_after(gate, 20)` スポーンで置換。既に await 中の notified() を起こすのは notify_one で十分 (値をためる必要があるなら notify_waiters/Notified::enable の挙動に注意)。
- **上流テストの llmCalls カウントは message_end(assistant) の個数で代用** (#15, 旧 #21) — Rust の stream fn はループ内で直接呼ばれないため呼び出し回数を直接観測できない。AtomicU32 をカウンタにして stream fn クロージャ内で fetch_add するのが素直。
- **閉じた `AgentMessage` union でのカスタムメッセージテスト** (#16, 旧 #21) — `CustomAgentMessages` は移植対象外 (divergence 済)。toolResult をスタンドインにし、converter が toolName=="notification" でフィルタ/マップする形で上流の挙動 (convertToLlm でのフィルタ/変換) を検証できる。
- **async fn の再帰はサイズ計算が循環してコンパイルできない** (#17, 旧 #22) — 再帰関数を薄いラッパーにして、実体を `Box::pin(inner(...))` で呼ぶ (skills.rs の `load_skills_from_dir_internal`)。

### データ・上流互換

- **上流の Map は挿入順。Rust では HashMap を使わない** (#18, 旧 #14) — `InMemoryCredentialStore` (credentials) と provider registry は上流では `Map` で `list`/`getProviders` が挿入順を返す。Rust 側は `Vec<(String, T)>` で保持している。既存キーの再 set は位置を維持 (Vec では in-place 更新)。新規 keyed コレクションの移植時も同様に。
- **serde_json の Map は順序付き (既定は BTreeMap)** (#19, 旧 #15) — `serde_json::Value` のオブジェクトは既定で BTreeMap (key ソート)。上流の Map 挿入順に依存する出力 (constrained-sampling の strict 変換が生成する `required` 配列など) は順序が変わる。providers は配列順に意味がないので控えめに寄せる方針。workspace で `preserve_order` feature を有効化するのは CBOR パリティへの影響が読めないため見送り。
- **repair_json の制御文字分岐で index が進まない** (#20, 旧 #19, 修正済み: 576c6fe) — in_string 内で `repaired.push(if ... { continue } else { c })` と書くと continue が `index += 1` を飛ばして無限ループする。if 文に分けて index を進めてから continue。テストは生の不正JSONを SSE data 行に流す形でしか露出しない (serde_json::from_str で先に弾かないこと)。
- **Responses SSE → AssistantMessageEventStream の契約** (#21, 旧 #17) — `processResponsesStream` は **Done/Error イベントを push しない** (上流呼び出し側が push する)。Rust テストで直接呼ぶときは、drive 完了後に `stream.end(None)` を呼ばないと EventIter が `done=false` のまま Pending で永遠に待つ。assert は `output.stop_reason` (finalize_response が書き込む) で行う。
- **ModelCompat union 化 (Box 包装)** (#22, 旧 #18) — `Model.compat` を per-API untagged enum `ModelCompat` にした。Box 包装 (clippy large-enum-difference)。使用側は `match model.compat.as_ref() { Some(ModelCompat::OpenaiCompletions(c)) => c, _ => return detected }` パターン。serde untagged なので JSON からは各 API 形がそのまま読める。テスト側は `ModelCompat::OpenaiCompletions(Box::new(...))` か `.into()`。
- **プロバイダ層の意図的な divergence** (#23, 旧 #16) —
  - `error_body.rs`: 上流は SDK エラーオブジェクトのフィールドを掘る (Mistral/openai/genai/Bedrock 形, pipe スニッフィング, class instance 判定)。Rust に SDK オブジェクトは無いので `normalize_provider_error(message, status, body)` が transport から受け取る形。`format_provider_error` / truncation は厳密互換。`provider-error-body-passthrough/regression.test.ts` は SDK レベルなので非移植。
  - `provider_retry.rs`: `retry-after` は数値のみパース (HTTP-date は指数バックオフにフォールバック)。jitter は fastrand。
  - `provider_env.rs`: Bun サンドボックスの /proc フォールバックは非移植。
  - `sanitize_surrogates` (text.rs): Rust の String は UTF-8 で unpaired surrogate を保持できないため恒等関数。上流と同じ呼び出し点を維持するための grep-parity 用。
  - `transport.rs`: 上流 `FetchFunction` は WHATWG Response を返すが、Rust は `FetchFn` トレイト + ストリーミング `FetchResponse`。既定実装は `ReqwestFetch` (rustls + gzip + stream)。

### ツール・デバッグ運用

- **bash ツールの安全な使い方 (ハング防止)** (#24, 旧 #4) — **バックグラウンドジョブ (`cmd &`) + `wait` は使わない**。タイムアウトは `perl -e 'alarm N; exec @ARGV' cmd...` でラップする (macOSにtimeoutはない)。出力はファイルにリダイレクトし、alarm で殺した後 `tail` する (ツールが stdout を返さないことがある)。
- **ハングのデバッグ手順 (macOS)** (#25, 旧 #3) — `cargo test` が止まったら**ビルドかテスト実行かを切り分ける**: テストバイナリのタイムスタンプとソースのタイムスタンプを比べる。テストバイナリを直接実行 (`--test-threads=1 --nocapture`) して特定のテストを絞る。`eprintln!` を「どの await まで進んだか」のマーカーとして入れるのが `sample` より速いことも多い。panic が握り潰されている可能性: `tokio::spawn` の JoinHandle を無視するとタスク内 panic が観測できない。

### harness/env/session 移植

- **tokio Command は親 env を継承する** (#26, 旧 #22) — 上流 `getShellEnv` の `{...process.env, ...baseEnv, ...extraEnv}` は「マージ結果」を返すが、Rust で `cmd.env(k,v)` を重ねるだけだと `inherit_env: false` でも親 env が残る。`inherit_env: Some(false)` のときは `cmd.env_clear()` を呼んでからマージ結果を適用する (`ShellExecOptions::inherit_env` は `Option<bool>` + `unwrap_or(true)` で上流 `?? true` と揃えた)。
- **同一トレイトメソッド名の曖昧呼び出し** (#27, 旧 #22) — `ExecutionEnv = FileSystem + Shell` で両者が `cleanup()` を持つと `env.cleanup()` が曖昧になる。呼び出し側は `FileSystem::cleanup(&env)` と明示する。
- **`select!` で future を再利用する今後のdrain** (#28, 旧 #22) — pipe読み取りを `&mut` 借用で future に組み込むと select! の落としていない branch 側から借用が残り E0499。読み取りは `tokio::spawn` の独立タスク + `Arc<Mutex<Vec<u8>>>` バッファにして、select! は child.wait のみを対象にするのが素直。
- **pi agent ハーネス自体が `PI_SESSION_FILE` 等を注入する** (#29, 旧 #22) — このリポジトリの開発環境 (pi の bash ツール) は `PI_SESSION_FILE`/`PI_CODING_AGENT`/`PI_SESSION_ID` を環境にセットする。env レイヤリングのテストはこの値が混入する前提で書く (上流テストも同一の変数名を使うので、期待値は上流テストのリテラル通りで正しい — テスト側の期待値を環境に合わせて変えないこと)。
- **edition 2024 で `std::env::set_var/remove_var` は unsafe** (#30, 旧 #22) — テストでも `unsafe { }` で囲む。
- **`.err().expect()` は clippy err_expect で落ちる** (#31, 旧 #22) — `.expect_err()` を使う。
- **上流の `yaml.parse` が throw する入力を再現する** (#32, 旧 #22) — `[invalid` のような未終了フロー構文はフラットパーサでは有効なスカラーになってしまう。`[`/`{` で始まり対応閉じがない値を `malformed` フラグにして、declared skill (SKILL.md) のみ `parse_failed` 診断、root .md は無視 — という上流挙動を再現した。python yaml で期待挙動を事前確認すると速い。
- **globset の gitignore 相似処理** (#33, 旧 #22) — スラッシュを含まないパターンは basename マッチ (`literal_separator(false)`)、`dir/` 付きは候補の前方一致も見る。upstream `ignore` クレートの完全互換ではないので、複雑な ignore パターンが出てきたら `ignore` クレートへの置換を検討。
- **session 移植の構造判断** (#34, 旧 #22) — 上流 `Entry` 判別ユニオンは「storage 割当エンベロープ (type/id/seq/parentId/timestamp) + `#[serde(flatten)]` payload enum」に分離。`findOpenOperations` の最新順は lane ごとの挿入順 Vec + id マップの二重管理で再現。Usage が unsigned のため上流の負の adjustment 記録はゼロ差分になり統計断言を調整 (divergence 記録)。repo の `share()` は `Arc` 内部共有ハンドルで同一 state を返す。clippy module_inception 回避のため session.rs 空スタブは削除し memory.rs に統合。
- **閉じた enum が上流の string 検証を吸収する** (#35) — reducer 移植で、上流が string で `compactionReason` の不正値検証 (`"manual"|"threshold"|"overflow"` 以外) をしている箇所は、Rust 側の enum 化で「存在検査」に縮む。divergence コメントをコードに残すこと。`ThinkingLevel` の未知文字列も同様に既定値へのフォールバックで表現。
- **同名型の二重定義に注意** (#36) — `ProvisionedEntry` は `session/types.rs` (serde版、`kind` フィールド付き) と `session/memory.rs` (storage版、id+payload のみ) に分かれており、それぞれ別の imports になる。reducer 等の新規モジュールは serde版を使い、テストの構築も `payload.kind()` から `kind` を導出する。
- **pure 関数の defensive clone は借用+出力クローンで置換** (#37) — 上流 `reduceLaneState` は `structuredClone` で入力保護しているが、Rust では入力を `&` で受け出力でクローンすれば同じ保証になる。ただし上流テストの「入力を mutate/alias しない」断言はそのまま `PartialEq` 比較で再現可能 (`LaneReductionInput` に `PartialEq` derive が必要)。
- **上流の `NEGATIVE_INFINITY` 歩行は `Option<u64>` で表現** (#38) — overflow recovery 検出の `seq > newestConsumedInputSequence` は「消費メッセージが無ければ常に true」が正。`u64::MAX` 等の sentinel を使うと逆意味になるので `Option` + `is_none_or` を使う。
- **テストでの `expect_err` は `Debug` を要求する** (#39) — `Result<T, E>::expect_err` は `T: Debug` を要求する。`AgentHarness` のような非 `Debug` 型を Ok 側に持つ結果は `match` で `Ok(_) => panic!()` にするか、エラー型だけを返すクロージャで検証する。
- **`'static` クロージャは借用を取れない** (#40) — `Vec<(&str, Box<dyn Fn() -> ...>)>` 型注釈はクロージャが外部参照を借用すると lifetime エラーになる。`Box<dyn Fn() -> ... + '_>` と借用 lifetime を明示する。
- **エンベロープ+flatten payload の二重 `type` タグ** (#45, 修正済み: 19e5663) — `Entry`/`LaneRecord`/`ProvisionedEntry` はエンベロープ構造体に `#[serde(rename="type")] kind` を持ちつつ `#[serde(flatten)]` payload (内部 `#[serde(tag="type")]` enum) を flatten すると、シリアライズ結果に `type` が2つ出て逆シリアルは必ず失敗する (「duplicate field type」)。serde_json の flatten は重複キーをマージしない。エンベロープ側の kind フィールドを廃止し `kind()` メソッド (payload.kind() 委譲) にするのが正。同じく payload enum に `rename_all_fields="camelCase"` を忘れると upstream ワイヤ形式 (`customType`/`runId`) とずれる。flatten 構造の serde パスは構築時に1回ラウンドトリップ試験を書くこと (既存のメモリテストだけでは露出しない)。
- **RPITIT トレイトは dyn 非対応** (#41) — `ExecutionEnv` (`impl Future` メソッド) は `&dyn ExecutionEnv` にできない。ツールは `<E: ExecutionEnv + ?Sized>` ジェネリクスで受け、内部可変性は `Arc<StdFsExecutionEnv>` 等で共有する。
- **upstream の擬似 promise キューは「登録区間内でロック取得」で置換** (#42) — `withFileMutationQueue` は registration promise の中で currentQueue に chained する。Rust では registration ロック内で `queue.lock().await` まで進め、ガードを持ったまま区間を抜けることで同じ順序保証になる。ガードを区間の外で取るとスケジューリング次第で逆転する。
- **オフセット系は1-indexed変換を忘れない** (#43) — read ツールの `offset` は upstream が `offset - 1` で 0-indexed に変換する。素通しすると 1 行ずれる (今回溶けたパターン: 一部のテストは通るが期待行がズレる)。
- **キューの順序テストはタイミング依存になりがち** (#44) — spawn 直後の2タスクの登録順は保証されない。upstream と同じ意味論 (先に登録した方が先に走る) を検証するなら、片方の開始を確実に観測してから次を投げる (Notify か十分な sleep)。フレークしたら連続10回。
- **同一 destination の create/fork 競合はプロセス内予約で防ぐ** (#46) — タイムスタンプ入りファイル名でも、async の存在チェックと発行の間に別 call が割り込むと同じ `{cwd, id}` のセッションが2つ publish され得る (upstream `claimCreateDestination` のコメント参照)。Rust では `Mutex<HashSet<String>>` + Drop で reservation を解放する RAII ガードにした (upstream try/finally 相当)。テストで同時実行を見たい場合は `tokio::join!` で2つの create を起動し成功1/失敗1 (already_exists) を断言する。時刻に依存するテストは repo を `with_clock` で固定時計にする。
- **sync 関数から async FS を呼ぶなら spawn_blocking+inline runtime** (#47) — `SessionStorage` トレイトは sync なので async `FileSystem::append_file` を呼ぶにはランタイム介入が必要。`block_in_place` は `#[tokio::test]` (current_thread flavor) で panic、`Handle::block_on` はランタイム内で「Cannot start a runtime from within a runtime」で panic。動くのは `Handle::spawn_blocking` でブロッキングプールに渡し、その中で `Builder::new_current_thread().enable_all().build()` したインラインランタイムで future を駆動する方法。ランタイム外の呼び出し元は std::fs にフォールバック。順序保証は state mutex が担う (upstream promise チェーン相当)。この制約のため `SessionStorage for JsonlSessionStorage` は `F: FileSystem + 'static` を要求する。
- **AsyncIterable の移植は lazy BoxStream で、ソース未来は poll まで解決しない** (#48) — search 移植で判明。upstream の async generator は最初の pull まで本体が走らない。Rust で素直に `BoxStream` を返すクロージャにすると呼び出し時に未来が生成されるだけで poll は遅延するが、クロージャ内で即 `boxed()` した future を `poll_unpin` する列挙状態 (Pending → Ready(stream) → Dynamic に差し替え) を明示的に持つ必要がある。また upstream の `throwIfAborted` は「各 readable 到着後」「各 entry 到着後」に走るので、ストリーム poll 内の対応する位置で `is_aborted()` を見る。素の Error throw (`AbortError` / `Duplicate sessionId`) は enum 化 (SearchError) が自然。
- **abort signal の型が層ごとに違うときは転送タスクでブリッジ** (#49) — e2e 移植で判明。pillar-agent の `AbortSignal` (waker ベース) と pillar-ai faux の `SharedAbort` (AtomicBool) は別型。`StreamFn` クロージャ内で `tokio::spawn(async move { signal.aborted().await; shared.abort(); })` の転送タスクを1本起動すれば両者を繋げられる。faux 側はチャンク毎に `is_aborted()` を見るのでポーリング間の遅延は許容。upstream は両者が同一の `AbortSignal` なのでこの層は存在しない (移植時のみの糊)。
- **TelemetryContext は dyn 非対応 (RPITIT)。スパンのネストは `SpanHandle::start_child` で** (#51) — telemetry 移植で判明。`TelemetryContext::start_span` は RPITIT のため `&dyn` にできず、typed starter 関数は `C: TelemetryContext` ジェネリクスで受ける (docs/INSTRUCTIONS.md #41 同型)。コールバック内でネストスパンを張るには `|span, starter|` の2引数クロージャを受け、`span.start_child(options, ...)` を使う (upstream `startChildSpan(span, ...)` 相当)。コンテキスト直呼びの `start_span` はルートスパンになり parent が切れないので注意。
- **Mistral SSE: `finish_reason: null` není stop reason a boundary hledej v bajtech** (#53) — mistral-conversations 移植で判明。上流 `if (choice.finish_reason)` は null/欠落を falsy としてスキップする (ストリームは継続)。null を「stop」として即時確定させると、途中チャンクで stop に落ちて後続データが無視される。また SSE 境界の検索はバイト列で行うこと — `String::from_utf8_lossy` はマルチバイト UTF-8 がチャンク境界で分断されると置換文字 (3バイト) に展開され、元のバイト列とインデックスがずれて `buffer.drain` が壊れる。区切り文字はすべて ASCII なのでバイト検索が安全。abort/timeout は fetch だけでなく body 読み取りにも掛ける (upstream `AbortSignal.any([signal, timeout])` 相当)。
- **Google 系の ADC パスはトークン発行まで移植しない** (#52) — google-vertex 移植で判明。上流は google-auth-library が ADC ファイルから OAuth2 JWT→access token 交換を行うが、Rust ポートはトークン発行まで実装しない。ADC パス (`~/.config/gcloud/application_default_credentials.json`) の存在確認 (`adc_credentials_available`) までを実装し、実際の Bearer トークンは呼び出し側が headers で渡す前提。API-key パス (Vertex Express) だけが完全に自己完結。

- **Bedrock eventstream はバイナリフレーム、CRC は長さフレーミングで代用** (#54) — bedrock-converse 移植で判明。ConverseStream のレスポンス体は SSE ではなく AWS eventstream 二進プロトコル ([4B total][4B headers-len][4B payload-len][headers][4B headers-CRC][payload][4B msg-CRC])。ヘッダ値型 7 (string) は 2 バイト長プレフィックス付き、他の型 (bool/byte/short/int/long/bytearray/timestamp/guid) は正しいワイアサイズでスキップすること (誤ったサイズは後続フレーム全体を壊す)。exception フレームは `:error-code` ヘッダにモデル化エラーコード。CRC 検証はせず長さフレーミングで信頼 (divergence)。
- **テストヘルパーの AssistantMessage は model をターゲットモデルに合わせる** (#58) — bedrock-converse パリティテストで判明。`assistant_message()` ヘルパーが `model` フィールドを base_model の ID にハードコードすると、`transform_messages` の `isSameModel` 判定が true になり tool call ID 正規化と redacted thinking 保持が発火しない (両者は cross-model のみ有効)。cross-model 挙動を検証するテストでは `assistant_message_from(&foreign_model, ...)` を使う。逆に same-model 挙動 (redacted reasoning 再生) を検証する場合はターゲットモデルと同じ ID で構築する。
- **ヘルパーにオプションを押し込むと別テストの前提を壊す** (#59) — bedrock-converse パリティテストで判明。`capture_payload` ヘルパーに `cache_retention: None` を焼き込むと、cache-point 注入を検証するテストが必ず失敗する (ヘルパー経由の全テストに設定が波及する)。ヘルパーは上流の共通前提 (abort + onPayload) だけを固定し、テスト固有の設定 (cache_retention など) は各テスト側で上書きする。ヘルパー変更後は全呼び出しテストを再実行すること。
- **global WebSocket をモックするテストは WsConn/WsConnFactory トレイト注入で置換** (#60) — openai-codex 移植で判明。上流は `vi.stubGlobal("WebSocket", MockWebSocket)` でイベントリスナー型の双方向ストリームを模倣する。Rust では `WsConnFactory::connect(url, headers, signal, timeout)` / `WsConn::{send, close, recv, is_reusable}` に抽象化し options 経由で注入する。モックの注意点: (a) スクリプト済みメッセージは `send` 内で同期 push し `terminal_sent` フラグを立てる — spawn+sleep 方式だと recv が終端を検出できずハングする、(b) `recv` はキュー空 & terminal_sent で `None` を返して clean close を模倣する、(c) `busy` な接続は上流同様 one-shot (プールに入れない) で扱う。tokio-tungstenite はデフォルト実装 (NativeWsConnFactory) とし、テストからのみモックに差し替える。
- **Bedrock ambient profile 判定は process env のみ。テストは process env を制御する** (#55) — 上流 `Boolean(getProviderEnvValue("AWS_PROFILE"))` は scoped options.env を含まない。options.env の AWS_PROFILE を ambient 判定に混ぜると「scoped profile が endpoint pinning を無効化する」バグになる。テストは `unsafe { std::env::remove_var/set_var }` で process env を制御し、終了時に復元する (#30 同様)。
- **上流の onPayload キャプチャパターンはコールバックで受ける** (#56) — 上流テストの `capturePayload` は「onPayload コールバック内で payload を取り、abort された request は送信されない」が前提。Rust テストで mock fetch の送信リクエストから payload を読むと「abort 済みなので request が飛ばない」で必ず失敗する。必ずコールバック内で `Arc<Mutex<Option<Value>>>` に格納する。
- **struct デフォルト値の上書きに注意** (#57) — テストヘルパーで `reasoning: High` を `..options` の**後**に置くと呼び出し側の Xhigh を上書きし「常に high」になる。デフォルト付けは struct 定義の**前**に置き、呼び出し側の値を `..options` で優先させる (Rust struct update 構文は後のフィールドが勝つ)。

## セッション運用の反省 (継継) (継続)

- **「修正した」は必ずテスト実行で確認してから言うこと**。デッドロック調査では「修正→別の箇所でハング」が連鎖した。
- フレークするテストは連続10回回して固定すること。実例: `rejects_late_publication` は 1/5 でしか落ちなかった (phase 1 が sender を消費する競合)。
- ユーザーが「止めて」と言ったら、同じ推論を繰り返さず手を止めること。
- この環境の command-guard 拡張 (`.pi/extensions/command-guard.ts`) の制約: 検証コマンド (`cargo test/check/clippy/fmt --check`) には bash ツールの `timeout` 秒指定が必須。`cargo test` は `-p <crate>` 付きで実行し、`--workspace` は `/guard allow-workspace` か確認ダイアログで明示許可。同じ検証コマンドの無変更再実行・同一ターン内の並列検証・1呼び出し内の複数 Git 操作はブロックされる。
